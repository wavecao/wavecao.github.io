<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>JVM调优和Linux命令</title>
    <url>/2020/ckdk09gon00003kvs6isd5hhz/</url>
    <content><![CDATA[<h1 id="JVM调优和Linux命令"><a href="#JVM调优和Linux命令" class="headerlink" title="JVM调优和Linux命令"></a>JVM调优和Linux命令</h1><h2 id="JVM调优"><a href="#JVM调优" class="headerlink" title="JVM调优"></a>JVM调优</h2><p>启动boot打好的包时：</p>
<p>java  -server  jvm的各种参数  包名</p>
<pre><code class="shell">java -server -Xms1024m -Xmx 1024 -XX:+UseG1GC 包名</code></pre>
<p>解释：-Xms堆内存初始大小，-Xmx堆内存最大大小，-XX:+UseG1GC使用G1垃圾回收器</p>
<h2 id="Linux命令之top"><a href="#Linux命令之top" class="headerlink" title="Linux命令之top"></a>Linux命令之top</h2><p><img src="http://image.cheerway.fun/blog/20200807/VFQBhat6WILl.png?imageslim" alt="mark"></p>
<p>精简版：uptime</p>
<p><img src="http://image.cheerway.fun/blog/20200807/EulGh7BhFd9P.png?imageslim" alt="mark"></p>
<h2 id="Linux-CPU-vmstat"><a href="#Linux-CPU-vmstat" class="headerlink" title="Linux-CPU-vmstat"></a>Linux-CPU-vmstat</h2><p><img src="http://image.cheerway.fun/blog/20200807/VI3vGY4E1V85.png?imageslim" alt="mark"></p>
<p>查看CPU</p>
<p>vmstat -n 2 3 第一个参数是采样的时间间隔数单位s，第二个参数是采样的次数</p>
<ul>
<li><p>procs</p>
<p><strong>r</strong>：运行和等待CPU时间片的进程数，原则上1核CPu的运行队列不要超过2，真个系统的运行队列不能超过总核数的2倍，否则表示系统压力过大</p>
<p><strong>b</strong>：等待资源的进程数，比如正在等待磁盘I/O，网络I/O等</p>
</li>
<li><p>cpu</p>
<p><strong>us</strong>：用户进程消耗cpu时间百分比，us高，用户进程消耗cpu时间多，如果长期大于50%，优化程序</p>
<p><strong>sy</strong>：内核进程消耗的cpu时间百分比</p>
<p><strong>us+sy</strong>：参考值为80%，如果大于80，说明可能存在cpu不足</p>
<p><strong>id</strong>：处于空闲的cpu百分比</p>
<p><strong>wa</strong>：系统等待IO的cpu时间百分比</p>
<p><strong>st</strong>：来自于一个虚拟机偷取的cpu时间的百分比</p>
</li>
<li><p>查看额外</p>
<ul>
<li>查看所有cpu核信息   mpstat -P ALL 2</li>
<li>每个进程使用cpu的用量分解信息   pidstat -u 1 -p 进程编号</li>
</ul>
</li>
</ul>
<h2 id="Linux-内存-free"><a href="#Linux-内存-free" class="headerlink" title="Linux-内存-free"></a>Linux-内存-free</h2><p><img src="http://image.cheerway.fun/blog/20200807/ycXosXfGrhal.png?imageslim" alt="mark"></p>
<p>pidstat -p 进程编号 -r 采样间隔秒数</p>
<p><img src="http://image.cheerway.fun/blog/20200807/Ei1CFJNtCFTb.png?imageslim" alt="mark"></p>
<h2 id="Linux-硬盘-df"><a href="#Linux-硬盘-df" class="headerlink" title="Linux-硬盘-df"></a>Linux-硬盘-df</h2><p><img src="C:%5CUsers%5C23537%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20200807161706356.png" alt="image-20200807161706356"></p>
<h2 id="Linux-磁盘IO-iostat"><a href="#Linux-磁盘IO-iostat" class="headerlink" title="Linux-磁盘IO-iostat"></a>Linux-磁盘IO-iostat</h2><p><img src="http://image.cheerway.fun/blog/20200807/bFdeYlISUTqR.png?imageslim" alt="mark"></p>
<p>磁盘I/O性能评估</p>
<p><strong>iostat -hdk 2 3</strong></p>
<ul>
<li>rkB/s每秒读取数据kb；</li>
<li>wkB/s每秒读写数据量kb</li>
<li>svctm I/O请求的平均服务时间，单位毫秒；</li>
<li>await I/O请求的平均等待时间，单位毫秒；值越小，性能越好；</li>
<li>==util== 一秒中又百分几的时间用于I/O操作，接近100%时，表示磁盘带宽跑满，需要优化程序或加磁盘</li>
<li>rkB/s，wkB/s根据系统该应用不同回有不同的值，担忧规律遵循：长期、超大数据读写，肯定不正常，需要优化程序读取。</li>
<li>svctm的值与await的值很接近，表示几乎没有I/O等待，磁盘性能好，如果await的值远高于svctm的值，则表示I/O队列等待太长，需要优化程序或更换更快磁盘</li>
</ul>
<p><strong>pidstat -d 采样间隔秒数 -p 进程号</strong></p>
<h2 id="Linux-网络IO-ifstat"><a href="#Linux-网络IO-ifstat" class="headerlink" title="Linux-网络IO-ifstat"></a>Linux-网络IO-ifstat</h2><p><img src="http://image.cheerway.fun/blog/20200807/eWITsK3NLu9t.png?imageslim" alt="mark"></p>
<h2 id="加入生产环境CPU占用过高，谈谈分析思路和定位？"><a href="#加入生产环境CPU占用过高，谈谈分析思路和定位？" class="headerlink" title="加入生产环境CPU占用过高，谈谈分析思路和定位？"></a>加入生产环境CPU占用过高，谈谈分析思路和定位？</h2><h4 id="案例步骤"><a href="#案例步骤" class="headerlink" title="案例步骤"></a>案例步骤</h4><ol>
<li><p>先用top命令找出cpu占比最高的</p>
</li>
<li><p>ps -ef或者jps进一步定位，得知是一个怎样的后台程序惹事</p>
<ul>
<li>jps -l</li>
<li>ps -ef|grep java|grep -v grep</li>
</ul>
</li>
<li><p>定位到具体线程或者代码</p>
<ul>
<li><p>ps -mp 进程编号 -o Thread,tid,time</p>
<p>定位到具体线程</p>
<p>-m ：显示所有的线程</p>
<p>-p pid 进程使用cpu的时间</p>
<p>-o：该参数后是用户自定义格式</p>
</li>
</ul>
</li>
<li><p>将需要的线程ID转换为16禁止格式（英文小写格式）</p>
<ul>
<li>printf “%x\n” 线程ID</li>
</ul>
</li>
<li><p>jstack 进程Id|grep tid(16进制线程id小写英文) -A60</p>
<p>查看运行轨迹，堆栈异常信息</p>
</li>
</ol>
]]></content>
      <categories>
        <category>JVM</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>Java多线程知识（未完待续）</title>
    <url>/2020/ckdk09gpd00063kvsapxlbuhb/</url>
    <content><![CDATA[<h1 id="Java多线程知识"><a href="#Java多线程知识" class="headerlink" title="Java多线程知识"></a>Java多线程知识</h1><h2 id="第一篇：基础篇"><a href="#第一篇：基础篇" class="headerlink" title="第一篇：基础篇"></a>第一篇：基础篇</h2><h3 id="第一章：进程与线程的基本概念"><a href="#第一章：进程与线程的基本概念" class="headerlink" title="第一章：进程与线程的基本概念"></a>第一章：进程与线程的基本概念</h3><h4 id="1-1-线程和进程的一些概念"><a href="#1-1-线程和进程的一些概念" class="headerlink" title="1.1 线程和进程的一些概念"></a>1.1 线程和进程的一些概念</h4><p><strong>进程的提出：</strong>由于<strong>批处理操作系统的指令运行方式仍然是串行的，内存中始终只有一个程序在运行</strong>，后面的程序需要等待前面的程序执行完成后才能开始执行，而前面的程序有时会由于I/O操作、网络等原因阻塞，所以<strong>批处理操作效率也不高</strong>。</p>
<p><strong>进程的概念：</strong>进程就是<strong>应用程序在内存中分配的空间，也就是正在运行的程序</strong>，各个进程之间互不干扰。同时进程保存着程序每一个时刻运行的状态。</p>
<p><strong>上下文切换：</strong>CPU为每个进程分配一个时间段，称作它的时间片。如果在时间片结束时进程还在运行，则暂停这个进程的运行，并且CPU分配给另一个进程。</p>
<blockquote>
<p>当进程暂停时，它会保存当前进程的状态（进程标识，进程使用的资源等），在下一次切换回来时根据之前保存的状态进行恢复，接着继续执行。</p>
</blockquote>
<p><strong>进程让操作系统的并发成为了可能！</strong></p>
<p><strong>线程的提出：</strong>虽然进程的出现，使得操作系统的性能大大提升，但是随着时间的推移，人们并不满足一个进程在一段时间只能做一件事情，如果一个进程有多个子任务时，只能逐个得执行这些子任务，很影响效率。</p>
<p><strong>线程的概念：</strong>让一个线程执行一个子任务，这样一个进程就包含了多个线程，每个线程负责一个单独的子任务。</p>
<p><strong>进程让操作系统的并发性成为了可能，而线程让进程的内部并发成为了可能。</strong></p>
<p>使用多线程的<strong>好处：</strong></p>
<ul>
<li>进程间的通信比较复杂，而线程间的通信比较简单，通常情况下，我们需要使用共享资源，这些资源在线程间的通信比较容易。</li>
<li>进程是重量级的，而线程是轻量级的，故多线程方式的系统开销更小。</li>
</ul>
<p><strong>进程和线程的区别：</strong></p>
<p><code>二者最本质的区别就是是否单独占有内存地址空间以及其他系统资源（比如I/O）</code></p>
<ul>
<li>进程单独占有一定的内存地址空间，所以进程间存在内存隔离，数据是分开的，数据共享复杂但是同步简单，各个进程之间互不干扰；而线程共享所属进程占有的内存地址空间和资源，数据共享简单，但是同步复杂。</li>
<li>进程单独占有一定的内存地址空间，一个进程出现问题不会影响其他进程，不影响主程序的稳定性，可靠性高；一个线程崩溃可能影响整个程序的稳定性，可靠性较低。</li>
<li>进程单独占有一定的内存地址空间，进程的创建和销毁不仅需要保存寄存器和栈信息，还需要资源的分配回收以及页调度，开销较大；线程只需要保存寄存器和栈信息，开销较小。</li>
</ul>
<p>另外一个重要区别是，<strong>进程是操作系统进行资源分配的基本单位，而线程是操作系统进行调度的基本单位</strong>，即CPU分配时间的单位 。</p>
<h4 id="1-2-上下文切换"><a href="#1-2-上下文切换" class="headerlink" title="1.2 上下文切换"></a>1.2 上下文切换</h4><p>上下文切换（有时也称做进程切换或任务切换）是指 CPU 从一个进程（或线程）切换到另一个进程（或线程）。上下文是指<strong>某一时间点 CPU 寄存器和程序计数器的内容。</strong></p>
<blockquote>
<p>寄存器是cpu内部的少量的速度很快的闪存，通常存储和访问计算过程的中间值提高计算机程序的运行速度。</p>
<p>程序计数器是一个专用的寄存器，用于表明指令序列中 CPU 正在执行的位置，存的值为正在执行的指令的位置或者下一个将要被执行的指令的位置，具体实现依赖于特定的系统。</p>
<p>举例说明 线程A - B</p>
<p>1.先挂起线程A，将其在cpu中的状态保存在内存中。</p>
<p>2.在内存中检索下一个线程B的上下文并将其在 CPU 的寄存器中恢复,执行B线程。</p>
<p>3.当B执行完，根据程序计数器中指向的位置恢复线程A。</p>
</blockquote>
<p>CPU通过为每个线程分配CPU时间片来实现多线程机制。CPU通过时间片分配算法来循环执行任务，当前任务执行一个时间片后会切换到下一个任务。</p>
<p>但是，在切换前会保存上一个任务的状态，以便下次切换回这个任务时，可以再加载这个任务的状态。所以任务从保存到再加载的过程就是一次上下文切换。</p>
<p>上下文切换通常是计算密集型的，意味着此操作会<strong>消耗大量的 CPU 时间，故线程也不是越多越好</strong>。如何减少系统中上下文切换次数，是提升多线程性能的一个重点课题。</p>
<h3 id="第二章：Java多线程入门类和接口"><a href="#第二章：Java多线程入门类和接口" class="headerlink" title="第二章：Java多线程入门类和接口"></a>第二章：Java多线程入门类和接口</h3><h4 id="2-1-Thread类和Runnable接口"><a href="#2-1-Thread类和Runnable接口" class="headerlink" title="2.1 Thread类和Runnable接口"></a>2.1 Thread类和Runnable接口</h4><p>JDk为我们提供了Thread类和Runnable接口实现自己的线程：</p>
<ul>
<li>继承<code>Thread</code>类，并重写<code>run()</code>方法</li>
<li>实现<code>Runnable</code>接口的<code>run()</code>方法</li>
</ul>
<h5 id="2-1-1-继承Thread类"><a href="#2-1-1-继承Thread类" class="headerlink" title="2.1.1 继承Thread类"></a>2.1.1 继承Thread类</h5><p>代码示例：</p>
<pre><code class="java">public class Demo {
    public static class MyThread extends Thread {
        @Override
        public void run() {
            System.out.println(&quot;MyThread&quot;);
        }
    }

    public static void main(String[] args) {
        Thread myThread = new MyThread();
        myThread.start();
    }
}</code></pre>
<p>必须要调用<code>start()</code>方法以后，线程才算启动！</p>
<blockquote>
<p>我们调用<code>start()</code>方法之后，虚拟机会创建出一个线程，此时线程只是就绪状态，只有当前线程分配到CPU时间片以后，再调用<code>run()</code>方法，线程开始执行任务。</p>
<p>不可以多次调用<code>start()</code> 方法，不然会报异常！</p>
</blockquote>
<h5 id="2-1-2-实现Runnable接口"><a href="#2-1-2-实现Runnable接口" class="headerlink" title="2.1.2 实现Runnable接口"></a>2.1.2 实现Runnable接口</h5><p>Runnable接口是一个函数式接口：</p>
<pre><code class="java">@FunctionalInterface
public interface Runnable {
    public abstract void run();
}</code></pre>
<p>这意味着可以使用函数式编程来简化代码：</p>
<pre><code class="java">public class Demo {
    public static class MyThread implements Runnable {
        @Override
        public void run() {
            System.out.println(&quot;MyThread&quot;);
        }
    }

    public static void main(String[] args) {
        new MyThread().run();

        // Java 8 函数式编程，可以省略MyThread类
        new Thread(() -&gt; {
            System.out.println(&quot;Java 8 匿名内部类&quot;);
        }).start();
    }
}</code></pre>
<h5 id="2-1-3-Thread类的构造方法"><a href="#2-1-3-Thread类的构造方法" class="headerlink" title="2.1.3 Thread类的构造方法"></a>2.1.3 Thread类的构造方法</h5><p><code>Thread</code>类是一个<code>Runnable</code>接口的实现类，我们来看看<code>Thread</code>类的源码。</p>
<p>查看<code>Thread</code>类的构造方法，发现其实是简单调用一个私有的<code>init</code>方法来实现初始化。<code>init</code>的方法签名：</p>
<pre><code class="java">// Thread类源码 

// 片段1 - init方法
private void init(ThreadGroup g, Runnable target, String name,
                      long stackSize, AccessControlContext acc,
                      boolean inheritThreadLocals)

// 片段2 - 构造函数调用init方法
public Thread(Runnable target) {
    init(null, target, &quot;Thread-&quot; + nextThreadNum(), 0);
}

// 片段3 - 使用在init方法里初始化AccessControlContext类型的私有属性
this.inheritedAccessControlContext = 
    acc != null ? acc : AccessController.getContext();

// 片段4 - 两个对用于支持ThreadLocal的私有属性
ThreadLocal.ThreadLocalMap threadLocals = null;
ThreadLocal.ThreadLocalMap inheritableThreadLocals = null;</code></pre>
<p>我们挨个来解释一下<code>init</code>方法的这些参数：</p>
<ul>
<li><p>g：线程组，指定这个线程是在哪个线程组下；</p>
</li>
<li><p>target：指定要执行的任务；</p>
</li>
<li><p>name：线程的名字，多个线程的名字是可以重复的。如果不指定名字，见片段2；</p>
</li>
<li><p>acc：见片段3，用于初始化私有变量<code>inheritedAccessControlContext</code>。</p>
<blockquote>
<p>这个变量有点神奇。它是一个私有变量，但是在<code>Thread</code>类里只有<code>init</code>方法对它进行初始化，在<code>exit</code>方法把它设为<code>null</code>。其它没有任何地方使用它。一般我们是不会使用它的，那什么时候会使用到这个变量呢？可以参考这个stackoverflow的问题：<a href="https://stackoverflow.com/questions/13516766/restrict-permissions-to-threads-which-execute-third-party-software" target="_blank" rel="noopener">Restrict permissions to threads which execute third party software</a>；</p>
</blockquote>
</li>
<li><p>inheritThreadLocals：可继承的<code>ThreadLocal</code>，见片段4，<code>Thread</code>类里面有两个私有属性来支持<code>ThreadLocal</code>，我们会在后面的章节介绍<code>ThreadLocal</code>的概念。</p>
</li>
</ul>
<p>实际情况下，我们大多是直接调用下面两个构造方法：</p>
<pre><code class="java">Thread(Runnable target)
Thread(Runnable target, String name)</code></pre>
<h5 id="2-1-4-Thread类的几个常用方法"><a href="#2-1-4-Thread类的几个常用方法" class="headerlink" title="2.1.4 Thread类的几个常用方法"></a>2.1.4 Thread类的几个常用方法</h5><ul>
<li>currentThread()：静态方法，返回对当前正在执行的线程对象的引用；</li>
<li>start()：开始执行线程的方法，java虚拟机会调用线程内的run()方法；</li>
<li>yield()：yield在英语里有放弃的意思，同样，这里的yield()指的是当前线程愿意让出对当前处理器的占用。这里需要注意的是，就算当前线程调用了yield()方法，程序在调度的时候，也还有可能继续运行这个线程的；</li>
<li>sleep()：静态方法，使当前线程睡眠一段时间；</li>
<li>join()：使当前线程等待另一个线程执行完毕之后再继续执行，内部调用的是Object类的wait方法实现的；</li>
</ul>
<h5 id="2-1-5-Thread类和Runnable接口的比较"><a href="#2-1-5-Thread类和Runnable接口的比较" class="headerlink" title="2.1.5 Thread类和Runnable接口的比较"></a>2.1.5 Thread类和Runnable接口的比较</h5><ul>
<li>由于Java“单继承，多实现”的特性，Runnable接口使用起来比Thread更灵活。</li>
<li>Runnable接口出现更符合面向对象，将线程单独进行对象的封装。</li>
<li>Runnable接口出现，降低了线程对象和线程任务的耦合性。</li>
<li>如果使用线程时不需要使用Thread类的诸多方法，显然使用Runnable接口更为轻量。</li>
</ul>
<p>所以，我们通常优先使用“实现<code>Runnable</code>接口”这种方式来自定义线程类。</p>
<h4 id="2-2-Callable、Future与FutureTask"><a href="#2-2-Callable、Future与FutureTask" class="headerlink" title="2.2 Callable、Future与FutureTask"></a>2.2 Callable、Future与FutureTask</h4><p>使用上述的两种方式实现多线程又一个弊端，就是<code>run()</code>方法没有返回值。</p>
<p>JDK给我们提供了<code>Callable</code>接口与<code>Future</code>类为我们解决这个问题，这也是所谓的“异步”模型。</p>
<h5 id="2-2-1-Callable接口"><a href="#2-2-1-Callable接口" class="headerlink" title="2.2.1 Callable接口"></a>2.2.1 Callable接口</h5><p><code>Callable</code>接口和<code>Runnable</code>接口类似，也是一个函数式接口，不同之处在于前者的<code>run()</code>方法有返回值，并且支持泛型。</p>
<pre><code class="java">@FunctionalInterface
public interface Callable&lt;V&gt; {
    V call() throws Exception;
}</code></pre>
<p><code>Callable</code>接口一般是配合线程池工具<code>ExcutorService</code>使用的，这里只介绍<code>ExecutorService</code>可以使用<code>submit</code>方法来让一个<code>Callable</code>接口执行。它会返回一个<code>Future</code>，我们后续的程序可以通过这个<code>Future</code>的<code>get</code>方法得到结果。</p>
<pre><code class="java">// 自定义Callable
class Task implements Callable&lt;Integer&gt;{
    @Override
    public Integer call() throws Exception {
        // 模拟计算需要一秒
        Thread.sleep(1000);
        return 2;
    }
    public static void main(String args[]){
        // 使用
        ExecutorService executor = Executors.newCachedThreadPool();
        Task task = new Task();
        Future&lt;Integer&gt; result = executor.submit(task);
        // 注意调用get方法会阻塞当前线程，直到得到结果。
        // 所以实际编码中建议使用可以设置超时时间的重载get方法。
        System.out.println(result.get()); 
    }
}</code></pre>
<p>输出结果：</p>
<pre><code class="java">2</code></pre>
<h5 id="2-2-2-Future接口"><a href="#2-2-2-Future接口" class="headerlink" title="2.2.2 Future接口"></a>2.2.2 Future接口</h5><p><code>Future</code>接口只有几个比较简单的方法：</p>
<pre><code class="java">public abstract interface Future&lt;V&gt; {
    public abstract boolean cancel(boolean paramBoolean);
    public abstract boolean isCancelled();
    public abstract boolean isDone();
    public abstract V get() throws InterruptedException, ExecutionException;
    public abstract V get(long paramLong, TimeUnit paramTimeUnit)
            throws InterruptedException, ExecutionException, TimeoutException;
}</code></pre>
<p><code>cancel</code>方法是试图取消一个线程的执行。</p>
<p>注意是<strong>试图</strong>取消，<strong>并不一定能取消成功</strong>。因为任务可能已完成、已取消、或者一些其它因素不能取消，存在取消失败的可能。<code>boolean</code>类型的返回值是“是否取消成功”的意思。参数<code>paramBoolean</code>表示是否采用中断的方式取消线程执行。</p>
<p>所以有时候，为了让任务有能够取消的功能，就使用<code>Callable</code>来代替<code>Runnable</code>。如果为了可取消性而使用 <code>Future</code>但又不提供可用的结果，则可以声明 <code>Future&lt;?&gt;</code>形式类型、并返回 <code>null</code>作为底层任务的结果。</p>
]]></content>
      <categories>
        <category>并发编程</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>单例模式详解</title>
    <url>/2020/ckd5pqa5b0000lwvsdod13m2i/</url>
    <content><![CDATA[<h1 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h1><h2 id="一、什么是单例模式"><a href="#一、什么是单例模式" class="headerlink" title="一、什么是单例模式"></a>一、什么是单例模式</h2><ul>
<li>单例类只能有一个实例</li>
<li>必须自行创建自己的唯一实例</li>
<li>向所有其他对象提供这一实例</li>
</ul>
<h2 id="二、单例模式的分类"><a href="#二、单例模式的分类" class="headerlink" title="二、单例模式的分类"></a>二、单例模式的分类</h2><p><strong>饿汉式：</strong>在类加载时候就创建单例（不管是否需要使用，都会先创建）</p>
<p><strong>懒汉式：</strong>在需要使用实例的时候才创建</p>
<h2 id="三、代码实现"><a href="#三、代码实现" class="headerlink" title="三、代码实现"></a>三、代码实现</h2><p>单例模式的核心思想：</p>
<ul>
<li>定义私有静态对象作为该类的唯一实例obj。</li>
<li>私有化构造函数，保证用户不可以直接通过构造函数创建该类实例，或直接访问该类实例。</li>
<li>定义一个公有的getInstance()方法去获得该类的唯一单例obj。</li>
</ul>
<h3 id="饿汉式："><a href="#饿汉式：" class="headerlink" title="饿汉式："></a>饿汉式：</h3><h4 id="方法一"><a href="#方法一" class="headerlink" title="方法一"></a>方法一</h4><p>静态常量：将唯一实例instance设置为静态常量</p>
<pre><code class="java">public class Singleton1 {
    private static final Singleton1 INSTANCE = new Singleton1();
    private Singleton1() {
        System.out.println(&quot;单例模式懒汉式(静态常量)——我被new出来了&quot;);
    }
    public static Singleton1 getInstance() {
        return INSTANCE;
    }
}</code></pre>
<p>特点：多线程下可以保证是唯一实例，但是造成了资源浪费，因为有时候我们可能不需要这个实例</p>
<h4 id="方法二"><a href="#方法二" class="headerlink" title="方法二"></a>方法二</h4><p>静态代码块：将类的实例化放在静态代码块中</p>
<pre><code class="java">public class Singleton2 {
    private static Singleton2 instance;
    static {
        instance = new Singleton2();
    }
    private Singleton2() {
        System.out.println(&quot;单例模式懒汉式(静态代码块)——我被new出来了&quot;);
    }

    public static Singleton2 getInstance() {
        return instance;
    }
}</code></pre>
<p>特点：与方法一相同</p>
<h4 id="方法三"><a href="#方法三" class="headerlink" title="方法三"></a>方法三</h4><p>静态内部类：</p>
<pre><code class="java">public class Singleton3 {
    private Singleton3() {
        System.out.println(&quot;单例模式懒汉式(静态内部类)——我被new出来了&quot;);
    }
    public static class SingletonInstance {
        private static final Singleton3 instance = new Singleton3();
    }
    public static Singleton3 getInstance() {
        return SingletonInstance.instance;
    }
}</code></pre>
<p>特点：Singleton3在加载的时候不会被实例化，而是在需要实例化时（调用getInstance()）,才会装载静态内部类，从而完成Singleton2的实例化。多线程下可以实现单例。</p>
<h3 id="懒汉式："><a href="#懒汉式：" class="headerlink" title="懒汉式："></a>懒汉式：</h3><h4 id="方法一-1"><a href="#方法一-1" class="headerlink" title="方法一"></a>方法一</h4><p>只判断一次+不加任何同步锁 多线程下不能实现单例，<strong>错误示范！</strong></p>
<pre><code class="java">public class Singleton4 {
    private static Singleton4 instance = null;
    private Singleton4() {
        System.out.println(&quot;实例被new出来了&quot;);
    }
    public static Singleton4 getInstance() {
        if (instance == null) {
            instance = new Singleton4();
        }
        return instance;
    }
}</code></pre>
<p>该方式<strong>不能保证</strong>高并发下的单个实例：比如线程1运行到if (instance == null)这行代码，还没来得及运行下一行创建实例的代码，此时线程2获得CPU时间片，恰好也运行到这个判断条件，就同样得到一个true结果，因此出现了两个实例！！！</p>
<h4 id="方法二-1"><a href="#方法二-1" class="headerlink" title="方法二"></a>方法二</h4><p>用synchronized对getInstance()方法加锁，可以实现单例</p>
<pre><code class="java">public class Singleton5 {
    private static Singleton5 instance = null;
    private Singleton5() {
        System.out.println(&quot;实例被new出来了&quot;);
    }
    public static synchronized Singleton5 getInstance() {
        if (instance == null) {
            instance = new Singleton5();
        }
        return instance;
    }
}</code></pre>
<p>缺点：这样给整个方法都加上了同步锁，即每次想得到一次单例都会给此方法加锁，这样使线程之间退化成串行化的执行。上面方法效率太低，我希望只在该方法执行第一次实例化的时候加锁，如果该单例已被new出，则不进入同步区域，直接返回即可。</p>
<h4 id="方法三-1"><a href="#方法三-1" class="headerlink" title="方法三"></a>方法三</h4><p>用synchronized对类加同步锁+双重检查  这个看测试结果似乎能实现单例，但其实没有volatile修饰的obj实例的这个类也是一个<strong>错误方法！</strong></p>
<pre><code class="java">public class Singleton6 {
    private static Singleton6 instance = null;
    private Singleton6() {
        System.out.println(&quot;实例被new出来了&quot;);
    }
    public static Singleton6 getInstance() {
        if (instance == null) {
            synchronized (Singleton6.class) {
                if (instance == null) {
                    instance = new Singleton6(); //error
                }
            }
        }
        return instance;
    }
}</code></pre>
<p>双重检查锁：</p>
<p>第一次检查instance是否被初始化（不去获得锁，只是查看是否满足加锁的条件而言），若已被初始化则返回。</p>
<p>第二次检查instance查看在加锁阻塞期间，是否已经有其他线程先一步对instance做了初始化。</p>
<p>这样看来似乎单例没问题呀，但其实这里有一个隐患，对构造函数来说，编译器为了优化进行指令重排序：</p>
<blockquote>
<p>实例化对象的那行代码（注释为为error的那行），实际上可以分解成以下三个步骤：</p>
<ol>
<li>分配内存空间</li>
<li>初始化对象</li>
<li>将对象指向刚分配的内存空间</li>
</ol>
<p>但是有些编译器为了性能的原因，可能会将第二步和第三步进行<strong>重排序</strong>，顺序就成了：</p>
<ol>
<li>分配内存空间</li>
<li>将对象指向刚分配的内存空间</li>
<li>初始化对象</li>
</ol>
</blockquote>
<p>因此整个过程可能会变成如下场景：</p>
<table>
<thead>
<tr>
<th>Step</th>
<th>Thread A</th>
<th>Thread B</th>
</tr>
</thead>
<tbody><tr>
<td>S1</td>
<td>检查到对象instance为空</td>
<td></td>
</tr>
<tr>
<td>S2</td>
<td>获取类的同步锁</td>
<td></td>
</tr>
<tr>
<td>S3</td>
<td>再次检查到instance为空</td>
<td></td>
</tr>
<tr>
<td>S4</td>
<td>为instance分配内存空间</td>
<td></td>
</tr>
<tr>
<td>S5</td>
<td>将instance指向刚分配的内存空间</td>
<td></td>
</tr>
<tr>
<td>S6</td>
<td></td>
<td>检查到对象instance不为空</td>
</tr>
<tr>
<td>S7</td>
<td></td>
<td>访问并返回instance（此时对象还未初始化，得到一个初始化未完成的对象）</td>
</tr>
<tr>
<td>S8</td>
<td>初始化instance</td>
<td></td>
</tr>
</tbody></table>
<p>表格中，S7时刻线程B对instance的访问，访问的是一个初始化未完成的对象，发生错误。 </p>
<h4 id="方法四"><a href="#方法四" class="headerlink" title="方法四"></a>方法四</h4><p>双重校验+volatile</p>
<p>双重校验+volatile 与上一个类不同的是，该类中对这个单例obj加了volatile关键字。这个关键字的作用是：使用了volatile关键字后，重排序被禁止，所有的写（write）操作都将发生在读（read）操作之前。在volatile和synchronized两端线程安全的保护下，保证多线程下单例的创建。</p>
<pre><code class="java">public class Singleton7 {
    private static volatile Singleton7 instance = null;
    private Singleton7() {
        System.out.println(&quot;实例被new出来了&quot;);
    }
    public static Singleton7 getInstance() {
        if (instance == null) {
            synchronized (Singleton7.class) {
                if (instance == null) {
                    instance = new Singleton7();
                }
            }
        }
        return instance;
    }
}</code></pre>
<p><strong>该方式是最完善的方式，面试的时候被问起直接上这种方式！</strong></p>
<h2 id="四、单例模式的使用场景"><a href="#四、单例模式的使用场景" class="headerlink" title="四、单例模式的使用场景"></a>四、单例模式的使用场景</h2><ul>
<li>需要频繁的进行创建和销毁的对象；</li>
<li>创建对象时耗时过多或耗费资源过多，但又经常用到的对象；</li>
<li>工具类对象；</li>
<li>频繁访问数据库或文件的对象。</li>
</ul>
<p>优点：该类只存在一个对象，节省了系统资源（省去了对象的频繁创建与销毁），提高了系统性能。</p>
<p>缺点：当想实例化一个单例类的时候，必须要记住使用相应的获取对象的方法，而不是使用new，可能会给其他开发人员造成困扰，特别是看不到源码的时候。例如本文中要创建一个单例类的实例时，你要知道getInstance（）这个函数名。</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>顺序队列和循环队列</title>
    <url>/2020/ck8481v1q0000q0tn6plnhbmr/</url>
    <content><![CDATA[<h3 id="什么是队列"><a href="#什么是队列" class="headerlink" title="什么是队列"></a>什么是队列</h3><p>对于数组来说，我们可以通过下标值拿到每一个具体的元素。但是有些时候我们需要一种限制存取顺序的数据结构，此时队列和栈便派上了用场。</p>
<p>队列与栈的不同之处在于，队列是先进先出（FIFO）而栈是后入先出（LIFO），其实<code>队列</code>二字“名副其实”，我们可以将其理解为日常生活中的排队：当我们在超市收银台结账时，肯定是排在前面的顾客先结账，然后依次是后面的顾客结账（不考虑某些人的插队行为）。</p>
<p><img src="http://image.cheerway.fun/Snipaste_2020-03-23_15-49-30.png" alt="mark"></p>
<p>如上图所示，队列是典型的 FIFO 数据结构。插入（insert）操作也称作入队（enqueue），新元素始终被添加在<code>队列的末尾</code>。 删除（delete）操作也被称为出队（dequeue)。 你只能移除<code>第一个元素</code>。</p>
<h3 id="顺序队列"><a href="#顺序队列" class="headerlink" title="顺序队列"></a>顺序队列</h3><p>顺序队列可以基于动态数组和指向头部的指针的方式实现的，是连续的一块内存空间。其结构如下图：</p>
<img src="http://image.cheerway.fun/blog/20200323/PfkWFmuOECIu.jpg?imageslim" alt="mark" style="zoom:50%;" />

<p>按照上述，其实现也十分简单，下面给出一种实现</p>
<pre><code class="java">public class MyQueue {
    //存储数据
    private List&lt;Integer&gt; data;
    //指向队列头部数据的指针
    private int p_start;

    public MyQueue() {
        this.data = data = new ArrayList&lt;Integer&gt;();
        this.p_start = 0;
    }

    //数据入列
    public Boolean enQueue(int x){
        data.add(x);
        return true;
    }

    //数据出列
    public Boolean deQueue(){
        if (isEmpty()){
            return false;
        }
        p_start++;
        return true;
    }

    //获取队列头的元素
    public int getFront(){
        return data.get(p_start);
    }

    //判断是否为空
    public Boolean isEmpty(){
        return p_start &gt;= data.size();
    }

    public static void main(String[] args) {
        MyQueue myQueue = new MyQueue();
        myQueue.enQueue(1);
        myQueue.enQueue(2);
        myQueue.enQueue(3);
        myQueue.enQueue(4);
        System.out.println(myQueue.getFront());
        myQueue.deQueue();
        System.out.println(myQueue.getFront());
    }
}</code></pre>
<h3 id="顺序队列的缺点"><a href="#顺序队列的缺点" class="headerlink" title="顺序队列的缺点"></a>顺序队列的缺点</h3><p>顺序队列的实现确实很简单，但是随着我们对一部分数据进行出队操作后，头部指针会渐渐后移，而头部指针前面的空间虽然已经被释放，但是它既不能被回收也不能被再次利用，造成了很大的空间浪费，我们当然可以每次队列满了以后将数据总体前移（如下图所示），这样虽然保证了空间不会被浪费，但是时间复杂度从O(1)变成了O(n)，也是非常大的性能开销。</p>
<img src="https://upload-images.jianshu.io/upload_images/2005932-580777cb33fcaf68.jpg?imageMogr2/auto-orient/strip|imageView2/2/w/1142/format/webp" style="zoom:50%;" />

<h3 id="循环队列"><a href="#循环队列" class="headerlink" title="循环队列"></a>循环队列</h3><p>上述通过数组来实现的队列，我们虽然进行了优化，但是当有空间无法利用时时，还是会进行一次数据搬移，性能也会收到影响，能否避免数据呢？答案是肯定的，看一下循环队列的解决思路。</p>
<p>循环队列就相当于一个圆环，数组可以想象成一条直线，我们把这条直线掰成一个圆环，就是循环队列，为了更形象的表示，可以看下图所示：</p>
<p><img src="http://image.cheerway.fun/blog/20200701/vavymwwzDzOe.jpg?imageslim" alt="mark"></p>
<p>循环队列中，我们需要两个指针，即head和tail分别指向队列的头和尾，当队列初始化时，head和tail都指向索引为0的位置。如果有数据入队，tail则后移一次，如果有数据出队，head则后移一次。</p>
<p>但是上述的过程会出现一系列问题：</p>
<ul>
<li>如果数组的大小为4，当tail指向3时，怎样让下一次入队时tail指向0（假设0位置可以插入）而不是指向4（指向4就造成数组下标越界了）？</li>
<li>当队列空或者满时，都存在head=tail，如何判断队列到底是空还是满？</li>
</ul>
<p>下面我们逐个解决</p>
<ol>
<li><p>关于指针越界的问题其实很简单，我们只需要每次在对tail或者head加1时进行一次取余操作，即</p>
<pre><code>(head + 1) % size;</code></pre></li>
<li><p>关于判断队列空和满状态的问题，我的思路是：舍弃一个存储空间。也就是说如果tail的下一个是head，那么现在tail指向的空间就不再存储数据。如果是队列不断进行出队操作，head渐渐“追上”tail时，会出现head=tail的情况，此时队列为空。</p>
</li>
</ol>
<p>总结一下：舍弃队列的最后一个存储空间，那么队列为空的标志是<code>(tail + 1) % size = head</code>，队列为满的标志是<code>head = tail</code>。</p>
<p>为了满足用户的需求，在实现时我们必须保证用户请求的空间数，因此具体到代码实现上，我们初始化队列时就初始化一个比用户申请空间大1的数组。下面给出一种代码实现：</p>
<pre><code class="java">public class MyCircularQueue {

    private int[] data;
    private int head;
    private int tail;
    private int size;

    /**
     * Initialize your data structure here. Set the size of the queue to be k.
     */
    public MyCircularQueue(int k) {
        //初始化一个比用户要求大1的数组
        size = k+1;
        data = new int[size];
        head = 0;
        tail = 0;
    }

    /**
     * Insert an element into the circular queue. Return true if the operation is successful.
     */
    public boolean enQueue(int value) {
        if (isFull()) {
            return false;
        }
        data[tail] = value;
        tail = (tail + 1) % size;
        return true;
    }

    /**
     * Delete an element from the circular queue. Return true if the operation is successful.
     */
    public boolean deQueue() {
        if (isEmpty()){
            return false;
        }
        head = (head + 1) % size;
        return true;
    }

    /**
     * Get the front item from the queue.
     */
    public int Front() {
        if (isEmpty()){
            return -1;
        }
        return data[head];
    }

    /**
     * Get the last item from the queue.
     */
    public int Rear() {
        if (isEmpty()){
            return -1;
        }
        //之所以要加上size是因为此处tail可能是0
        return data[(tail - 1 + size) % size];
    }

    /**
     * Checks whether the circular queue is empty or not.
     */
    public boolean isEmpty() {
        return head == tail;
    }

    /**
     * Checks whether the circular queue is full or not.
     * 如果仅仅使用tail == head来判断队列是否满，则无法判断到底是空还是满
     * 因此要牺牲最后一个存储单元。即如果tail的下一个是head，就认为满了
     */
    public boolean isFull() {
        return (tail + 1) % size == head;
    }

}
</code></pre>
<h3 id="队列的其他形式"><a href="#队列的其他形式" class="headerlink" title="队列的其他形式"></a>队列的其他形式</h3><p>队列其实还有链式队列，无界队列，阻塞队列、并发队列等等，我们以后再聊！</p>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue组件基础</title>
    <url>/2020/ck7pivht30000a8tngd4bfysi/</url>
    <content><![CDATA[<h2 id="Vue组件基础"><a href="#Vue组件基础" class="headerlink" title="Vue组件基础"></a>Vue组件基础</h2><h3 id="Vue的组件化思想"><a href="#Vue的组件化思想" class="headerlink" title="Vue的组件化思想"></a>Vue的组件化思想</h3><p>组件化是Vue中很重要的思想：</p>
<ul>
<li>它提供了一种抽象，让我们可以开发出一个个独立可复用的小组件来构造我们的应用</li>
<li>任何的应用都会被抽象成一颗组件树</li>
</ul>
<p><img src="http://image.cheerway.fun/blog/20200309/2UYo8tWiUW68.png?imageslim" alt="mark"></p>
<p>组件化思想的应用：</p>
<ul>
<li>有了组件化的思想，我们在之后的开发中就要充分的利用它。</li>
<li>尽可能的将页面拆分成一个个小的、可复用的组件。</li>
<li>这样让我们的代码更加方便组织和管理，并且扩展性也更强。</li>
</ul>
<h3 id="注册组件"><a href="#注册组件" class="headerlink" title="注册组件"></a>注册组件</h3><h4 id="全局注册"><a href="#全局注册" class="headerlink" title="全局注册"></a>全局注册</h4><ul>
<li>Vue.component(‘组件名称’, { })     第1个参数是标签名称，第2个参数是一个选项对象</li>
<li><strong>全局组件</strong>注册后，任何<strong>vue实例</strong>都可以用</li>
</ul>
<pre><code class="html">&lt;div id=&quot;example&quot;&gt;
  &lt;!-- 2、 组件使用 组件名称 是以HTML标签的形式使用  --&gt;  
  &lt;my-component&gt;&lt;/my-component&gt;
&lt;/div&gt;
&lt;script&gt;
    //   注册组件 
    // 1、 my-component 就是组件中自定义的标签名
    Vue.component(&#39;my-component&#39;, {
      template: &#39;&lt;div&gt;A custom component!&lt;/div&gt;&#39;
    })

    // 创建根实例
    new Vue({
      el: &#39;#example&#39;
    })

&lt;/script&gt;</code></pre>
<h4 id="局部注册"><a href="#局部注册" class="headerlink" title="局部注册"></a>局部注册</h4><pre><code class="html">&lt;div id=&quot;app&quot;&gt;
      &lt;my-component&gt;&lt;/my-component&gt;
  &lt;/div&gt;


&lt;script&gt;
    // 定义组件的模板
    var Child = {
      template: &#39;&lt;div&gt;A custom component!&lt;/div&gt;&#39;
    }
    new Vue({
      //局部注册组件  
      components: {
        // &lt;my-component&gt; 将只在父模板可用  一定要在实例上注册了才能在html文件中使用
        &#39;my-component&#39;: Child
      }
    })
 &lt;/script&gt;</code></pre>
<p><strong>局部注册的组件只能在注册它的Vue实例中使用</strong></p>
<h4 id="组件注意事项"><a href="#组件注意事项" class="headerlink" title="组件注意事项"></a>组件注意事项</h4><ul>
<li>组件参数data必须是一个函数，同时要求改函数返回一个对象</li>
<li>组件模板必须是单个根元素，当需要写多个HTML标签时，需在最外层套一个div</li>
<li>组件模板的内容可以是模板字符串</li>
<li>组件可以使用多次</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        &lt;div id=&quot;app&quot;&gt;
            &lt;cpn&gt;&lt;/cpn&gt;
            &lt;cpn&gt;&lt;/cpn&gt;
            &lt;cpn&gt;&lt;/cpn&gt;
        &lt;/div&gt;
        &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
        &lt;script&gt;
            Vue.component(&#39;cpn&#39;, {
                template: &#39;&lt;button @click=&quot;handle&quot;&gt;点击了{{counter}}次&lt;/button&gt;&#39;,
                data() {
                    return {
                        counter: 0
                    }
                },
                methods:{
                    handle() {
                        this.counter++;
                    }
                }
            })
          const app = new Vue({
          el: &quot;#app&quot;,
            data: {

            }
          });
        &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;</code></pre>
<p>如上的代码中，名为<code>cpn</code>的组件被使用了三次，从运行效果可以看出，每个组件的counter变量是独立的</p>
<p><img src="http://image.cheerway.fun/blog/20200309/611k8DchY8ba.png?imageslim" alt="mark"></p>
<h4 id="模板的分离写法"><a href="#模板的分离写法" class="headerlink" title="模板的分离写法"></a>模板的分离写法</h4><p>上述注册组件的方法，我们将template模板放在了注册组件的内部，这样做会带来两个坏处</p>
<ol>
<li>没有代码补全，撰写模板的过程更麻烦</li>
<li>代码看起来更加凌乱</li>
</ol>
<p>因此一般我们会采用模板的分离写法，nVue提供了两种方案来定义HTML模块内容：</p>
<ul>
<li><p>使用<code>&lt;script&gt;</code>标签</p>
</li>
<li><p>使用<code>&lt;template&gt;</code>标签</p>
</li>
</ul>
<p><img src="http://image.cheerway.fun/blog/20200309/nfLi6kjGQVmz.png?imageslim" alt="mark"></p>
<p><img src="http://image.cheerway.fun/blog/20200309/ha9Pr7SxHYuB.png?imageslim" alt="mark"></p>
<h3 id="组件之间的通信"><a href="#组件之间的通信" class="headerlink" title="组件之间的通信"></a>组件之间的通信</h3><h4 id="父组件向子组件传值"><a href="#父组件向子组件传值" class="headerlink" title="父组件向子组件传值"></a>父组件向子组件传值</h4><ul>
<li>父组件发送的形式是以属性的形式绑定值到子组件身上。</li>
<li>然后子组件用属性props接收</li>
<li>在props中使用驼峰形式，模板中需要使用短横线的形式字符串形式的模板中没有这个限制</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;UTF-8&quot;&gt;
        &lt;title&gt;Title&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        &lt;div id=&quot;app&quot;&gt;
            &lt;cpn :c-movies=&quot;movies&quot; :c-message=&quot;message&quot;&gt;&lt;/cpn&gt;
        &lt;/div&gt;
        &lt;template id=&quot;cpn&quot;&gt;
            &lt;div&gt;
                &lt;h2&gt;{{cMessage}}&lt;/h2&gt;
                &lt;div&gt;
                    &lt;ul&gt;
                        &lt;li v-for=&quot;item in cMovies&quot;&gt;{{item}}&lt;/li&gt;
                    &lt;/ul&gt;
                &lt;/div&gt;
            &lt;/div&gt;
        &lt;/template&gt;
        &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
        &lt;script&gt;
            const app = new Vue({
                el: &quot;#app&quot;,
                data: {
                    message: &#39;你好啊&#39;,
                    movies: [&#39;星际穿越&#39;, &#39;火星救援&#39;, &#39;星际迷航&#39;, &#39;星球大战&#39;]
                },
                components: {
                    cpn: {
                        template: &#39;#cpn&#39;,
                        data() {
                            return {

                            }
                        },
                        methods: {

                        },
                        props: {
                            cMovies: {
                                type: Array,
                                default () {
                                    return [&#39;随便什么电影&#39;]
                                }
                            },
                            cMessage: {
                                type: String,
                                default: &#39;aaaaaa&#39;
                            }
                        }
                    }
                },
            });
        &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>上面的代码中，props属性采用的是对象写法，好处就是可以进行类型校验，默认值设置等操作，而另外的数组写法则不能进行这些操作：</p>
<pre><code class="html">props: [&#39;cMovies&#39;, &#39;cMessage&#39;]</code></pre>
<h4 id="子组件向父组件传值"><a href="#子组件向父组件传值" class="headerlink" title="子组件向父组件传值"></a>子组件向父组件传值</h4><ul>
<li>子组件用<code>$emit()</code>触发事件</li>
<li><code>$emit()</code>  第一个参数为 自定义的事件名称     第二个参数为需要传递的数据</li>
<li>父组件用v-on 监听子组件的事件</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
         &lt;div id=&quot;app&quot;&gt;
            &lt;div :style=&#39;{fontSize: fontSize + &quot;px&quot;}&#39;&gt;{{pmsg}}&lt;/div&gt;
             &lt;!-- 父组件用v-on 监听子组件的事件
                这里 enlarge-text  是从 $emit 中的第一个参数对应   handle 为对应的事件处理函数    
            --&gt;    
            &lt;menu-item :parr=&#39;parr&#39; @enlarge-text=&#39;handle($event)&#39;&gt;&lt;/menu-item&gt;
          &lt;/div&gt;
            &lt;template id=&quot;cpn&quot;&gt;
                &lt;div&gt;
                    &lt;ul&gt;
                      &lt;li :key=&#39;index&#39; v-for=&#39;(item,index) in parr&#39;&gt;{{item}}&lt;/li&gt;
                    &lt;/ul&gt;
                        &lt;!-- 子组件用$emit()触发事件
                        第一个参数为 自定义的事件名称   第二个参数为需要传递的数据 --&gt;  
                    &lt;button @click=&#39;$emit(&quot;enlarge-text&quot;, 5)&#39;&gt;扩大父组件中字体大小&lt;/button&gt;
                    &lt;button @click=&#39;$emit(&quot;enlarge-text&quot;, 10)&#39;&gt;扩大父组件中字体大小&lt;/button&gt;
                  &lt;/div&gt;
            &lt;/template&gt;
          &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
          &lt;script type=&quot;text/javascript&quot;&gt;
            var vm = new Vue({
              el: &#39;#app&#39;,
              data: {
                pmsg: &#39;父组件中内容&#39;,
                parr: [&#39;apple&#39;,&#39;orange&#39;,&#39;banana&#39;],
                fontSize: 10
              },
              methods: {
                handle: function(val){
                  // 扩大字体大小
                  this.fontSize += val;
                }
              },
                    components:{
                        &#39;menu-item&#39;: {
                            props: [&#39;parr&#39;],
                            template: &#39;#cpn&#39;
                        }
                    }
            });
          &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h4 id="兄弟间的传值"><a href="#兄弟间的传值" class="headerlink" title="兄弟间的传值"></a>兄弟间的传值</h4><ul>
<li>兄弟之间传递数据需要借助于事件中心，通过事件中心传递数据   <ul>
<li>提供事件中心    var hub = new Vue()</li>
</ul>
</li>
<li>传递数据方，通过一个事件触发hub.$emit(方法名，传递的数据)</li>
<li>接收数据方，通过mounted(){} 钩子中  触发hub.$on()方法名</li>
<li>销毁事件 通过hub.$off()方法名销毁之后无法进行传递数据</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
         &lt;div id=&quot;app&quot;&gt;
            &lt;div&gt;父组件&lt;/div&gt;
            &lt;div&gt;
              &lt;button @click=&#39;handle&#39;&gt;销毁事件&lt;/button&gt;
            &lt;/div&gt;
            &lt;test-tom&gt;&lt;/test-tom&gt;
            &lt;test-jerry&gt;&lt;/test-jerry&gt;
          &lt;/div&gt;
          &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
          &lt;script type=&quot;text/javascript&quot;&gt;
            /*
              兄弟组件之间数据传递
            */
            //1、 提供事件中心
            var hub = new Vue();

            Vue.component(&#39;test-tom&#39;, {
              data: function(){
                return {
                  num: 0
                }
              },
              template: `
                &lt;div&gt;
                  &lt;div&gt;TOM:{{num}}&lt;/div&gt;
                  &lt;div&gt;
                    &lt;button @click=&#39;handle&#39;&gt;点击&lt;/button&gt;
                  &lt;/div&gt;
                &lt;/div&gt;
              `,
              methods: {
                handle: function(){
                  //2、传递数据方，通过一个事件触发hub.$emit(方法名，传递的数据)   触发兄弟组件的事件
                  hub.$emit(&#39;jerry-event&#39;, 2);
                }
              },
              mounted: function() {
               // 3、接收数据方，通过mounted(){} 钩子中  触发hub.$on(方法名
                hub.$on(&#39;tom-event&#39;, (val) =&gt; {
                  this.num += val;
                });
              }
            });
            Vue.component(&#39;test-jerry&#39;, {
              data: function(){
                return {
                  num: 0
                }
              },
              template: `
                &lt;div&gt;
                  &lt;div&gt;JERRY:{{num}}&lt;/div&gt;
                  &lt;div&gt;
                    &lt;button @click=&#39;handle&#39;&gt;点击&lt;/button&gt;
                  &lt;/div&gt;
                &lt;/div&gt;
              `,
              methods: {
                handle: function(){
                  //2、传递数据方，通过一个事件触发hub.$emit(方法名，传递的数据)   触发兄弟组件的事件
                  hub.$emit(&#39;tom-event&#39;, 1);
                }
              },
              mounted: function() {
                // 3、接收数据方，通过mounted(){} 钩子中  触发hub.$on()方法名
                hub.$on(&#39;jerry-event&#39;, (val) =&gt; {
                  this.num += val;
                });
              }
            });
            var vm = new Vue({
              el: &#39;#app&#39;,
              data: {

              },
              methods: {
                handle: function(){
                  //4、销毁事件 通过hub.$off()方法名销毁之后无法进行传递数据  
                  hub.$off(&#39;tom-event&#39;);
                  hub.$off(&#39;jerry-event&#39;);
                }
              }
            });
          &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="组件插槽"><a href="#组件插槽" class="headerlink" title="组件插槽"></a>组件插槽</h3><h4 id="匿名插槽"><a href="#匿名插槽" class="headerlink" title="匿名插槽"></a>匿名插槽</h4><ul>
<li><p>组件最大的特性就是复用性，而插槽作为组件的扩展大大提高了组件的可复用能力</p>
</li>
<li><p>在vue组件的模板中，可用<code>&lt;slot&gt;&lt;/slot&gt;</code>添加一个插槽，插槽内的内容是默认值，当没有被覆盖时，将显示默认值</p>
</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        &lt;div id=&quot;app&quot;&gt;
            &lt;!-- 此处组件标签内没有其他内容，因此显示默认内容 --&gt;
            &lt;cpn&gt;&lt;/cpn&gt;
            &lt;!-- 此处利用span标签覆盖了插槽的默认内容 --&gt;
            &lt;cpn&gt;&lt;span&gt;这里是一些文字&lt;/span&gt;&lt;/cpn&gt;
            &lt;!-- 组件标签里的所有内容都会作为插槽内容显示，因此此处的三个标签都会显示 --&gt;
            &lt;cpn&gt;
                &lt;b&gt;加粗文字&lt;/b&gt;
                &lt;i&gt;斜体文字&lt;/i&gt;
                &lt;span&gt;这里是一些文字&lt;/span&gt;
            &lt;/cpn&gt;
        &lt;/div&gt;
        &lt;template id=&quot;cpn&quot;&gt;
            &lt;div&gt;
                &lt;h2&gt;我是组件标题&lt;/h2&gt;
                &lt;p&gt;我是组件的文本内容&lt;/p&gt;
                &lt;slot&gt;&lt;button type=&quot;button&quot;&gt;这是按钮&lt;/button&gt;&lt;/slot&gt;
            &lt;/div&gt;
        &lt;/template&gt;
        &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
        &lt;script&gt;
            const app = new Vue({
                el: &#39;#app&#39;,
                data: {

                },
                components:{
                    cpn: {
                        template:&#39;#cpn&#39;
                    }
                }
            })
        &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h4 id="具名插槽"><a href="#具名插槽" class="headerlink" title="具名插槽"></a>具名插槽</h4><ul>
<li>当有多个插槽时，我们要想覆盖特定的某个插槽就必须使用名字，这就是具名插槽</li>
<li>使用 <code>&lt;slot&gt;</code> 中的 “name” 属性绑定元素</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        &lt;div id=&quot;app&quot;&gt;
            &lt;cpn&gt;
                &lt;button type=&quot;button&quot; slot=&quot;left&quot;&gt;按钮&lt;/button&gt;
                &lt;span slot=&quot;center&quot;&gt;&lt;b&gt;这是中间的标题&lt;/b&gt;&lt;/span&gt;
                &lt;a href=&quot;&quot; slot=&quot;right&quot;&gt;一个链接&lt;/a&gt;
            &lt;/cpn&gt;
        &lt;/div&gt;
        &lt;template id=&quot;cpn&quot;&gt;
            &lt;div&gt;
                &lt;slot name=&quot;left&quot;&gt;&lt;span&gt;左边&lt;/span&gt;&lt;/slot&gt;
                &lt;slot name=&quot;center&quot;&gt;&lt;span&gt;中间&lt;/span&gt;&lt;/slot&gt;
                &lt;slot name=&quot;right&quot;&gt;&lt;span&gt;右边&lt;/span&gt;&lt;/slot&gt;
            &lt;/div&gt;
        &lt;/template&gt;
        &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
        &lt;script&gt;
            const app = new Vue({
                el: &#39;#app&#39;,
                data: {

                },
                components:{
                    cpn: {
                        template:&#39;#cpn&#39;
                    }
                }
            })
        &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h4 id="作用域插槽"><a href="#作用域插槽" class="headerlink" title="作用域插槽"></a>作用域插槽</h4><ul>
<li>父组件对子组件加工处理</li>
<li>既可以复用子组件的slot，又可以使slot内容不一致</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        &lt;div id=&quot;app&quot;&gt;
            &lt;!-- 默认是无序列表 --&gt;
            &lt;cpn&gt;&lt;/cpn&gt;
            &lt;!-- 此处将内容改为每个元素中间用“-”连接 --&gt;
            &lt;cpn&gt;
                &lt;template v-slot=&quot;slot&quot;&gt;
                    &lt;span v-for=&quot;item in slot.data&quot;&gt;{{item}} - &lt;/span&gt;
                &lt;/template&gt;
            &lt;/cpn&gt;
            &lt;!-- 此处将内容改为每个元素中间用“*”连接 --&gt;
            &lt;cpn&gt;
                &lt;template v-slot=&quot;slot&quot;&gt;
                    &lt;span&gt;{{slot.data.join(' * ')}}&lt;/span&gt;
                &lt;/template&gt;
            &lt;/cpn&gt;
        &lt;/div&gt;
        &lt;template id=&quot;cpn&quot;&gt;
            &lt;div&gt;
                &lt;slot :data=&quot;items&quot;&gt;
                    &lt;ul&gt;
                        &lt;li v-for=&quot;item in items&quot;&gt;{{item}}&lt;/li&gt;
                    &lt;/ul&gt;
                &lt;/slot&gt;
            &lt;/div&gt;
        &lt;/template&gt;
        &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
        &lt;script&gt;
            const app = new Vue({
                el: &#39;#app&#39;,
                data: {

                },
                components:{
                    cpn: {
                        template:&#39;#cpn&#39;,
                        data() {
                            return {
                                items: [&#39;A&#39;, &#39;B&#39;, &#39;C&#39;, &#39;D&#39;, &#39;E&#39;, &#39;F&#39;, &#39;G&#39;]
                            }
                        }
                    }
                }
            })
        &lt;/script&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="父子组件通信的练习"><a href="#父子组件通信的练习" class="headerlink" title="父子组件通信的练习"></a>父子组件通信的练习</h3><p>在接下来的实例中，我将模拟在有网络请求的情况下，用户点击不同的分类tab时显示不通内容的案例。</p>
<ul>
<li>父组件首先模拟向后台请求数据，请求到数据后显示tab列表并显示默认的数据</li>
<li>用户可以点击按钮，点击后子组件通知父组件模拟请求对应的数据</li>
<li>父组件请求到数据后传给子组件并显示</li>
</ul>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;meta charset=&quot;utf-8&quot;&gt;
        &lt;title&gt;&lt;/title&gt;
    &lt;/head&gt;
    &lt;style type=&quot;text/css&quot;&gt;
        .active{
          background-color: #00aaff;
        }
        .btn{
            margin-right: 10px;
            border: 0;
            border-radius: 2px;
            padding-left: 10px;
            padding-right: 10px;
            height: 30px;
        }
    &lt;/style&gt;
    &lt;body&gt;
        &lt;div id=&quot;app&quot;&gt;
            &lt;cpn :categories=&#39;categories&#39; :items=&#39;items&#39; @btnclick=&#39;changeitems&#39;&gt;&lt;/cpn&gt;
        &lt;/div&gt;
    &lt;/body&gt;
    &lt;template id=&quot;cpn&quot;&gt;
        &lt;div&gt;
            &lt;button type=&quot;button&quot; class=&quot;btn&quot; v-for=&quot;(category, index) in categories&quot; 
            @click=&quot;btnclick(category, index)&quot; :class=&quot;{active: currentIndex == index}&quot;&gt;
            {{category.name}}
            &lt;/button&gt;
            &lt;br&gt;
            &lt;ul&gt;
                &lt;li v-for=&quot;item in items&quot;&gt;{{item}}&lt;/li&gt;
            &lt;/ui&gt;
        &lt;/div&gt;
    &lt;/template&gt;
    &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
    &lt;script&gt;
        const cpn = {
            template: &#39;#cpn&#39;,
            props: {
                items: {
                    type: Array,
                    default() {
                        return [&#39;无数据&#39;]
                    }
                },
                categories: {
                    type: Array,
                    required: true
                }
            },
            methods: {
                btnclick(category, index){
                    //console.log(category);
                    this.currentIndex = index;
                    this.$emit(&#39;btnclick&#39;, category);
                },
            },
            data() {
                return {
                    currentIndex: 0
                }
            }
        };
        const app = new Vue({
            el: &#39;#app&#39;,
            data: {
                categories: [
                    {
                        id: &#39;movies&#39;,
                        name: &#39;电影&#39;
                    },
                    {
                        id: &#39;fruits&#39;,
                        name: &#39;水果&#39;
                    },
                    {
                        id: &#39;musics&#39;,
                        name: &#39;音乐&#39;
                    }
                ],
                //ff
                lists: {
                    movies: [&#39;星际穿越&#39;, &#39;火星救援&#39;, &#39;星际迷航&#39;, &#39;星球大战&#39;],
                    fruits: [&#39;苹果&#39;, &#39;香蕉&#39;, &#39;草莓&#39;, &#39;鸭梨&#39;],
                    musics: [&#39;生命因你而火热&#39;, &#39;没有理想的人不伤心&#39;, &#39;你要跳舞吗&#39;]
                },
                items: []
            },
            components: {
                cpn
            },
            mounted() {
                //模拟网络请求和数据绑定
                this.items = this.lists.movies;
            },
            methods: {
                changeitems(category) {
                    //模拟网络请求和数据绑定
                    this.items = this.lists[category.id];
                }
            }
        })

    &lt;/script&gt;
&lt;/html&gt;
</code></pre>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>Vue</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue入门——基础语法</title>
    <url>/2020/ck7d2hz3p0000agtn4vjsaopj/</url>
    <content><![CDATA[<h2 id="Vue入门——基础语法"><a href="#Vue入门——基础语法" class="headerlink" title="Vue入门——基础语法"></a>Vue入门——基础语法</h2><h3 id="Vue是什么？"><a href="#Vue是什么？" class="headerlink" title="Vue是什么？"></a>Vue是什么？</h3><p>Vue (读音 /vjuː/，类似于 <strong>view</strong>) 是一套用于构建用户界面的<strong>渐进式框架</strong>。与其它大型框架不同的是，Vue 被设计为可以自底向上逐层应用。Vue 的核心库只关注视图层，不仅易于上手，还便于与第三方库或既有项目整合。另一方面，当与<a href="https://vuejs.bootcss.com/guide/single-file-components.html" target="_blank" rel="noopener">现代化的工具链</a>以及各种<a href="https://github.com/vuejs/awesome-vue#libraries--plugins" target="_blank" rel="noopener">支持类库</a>结合使用时，Vue 也完全能够为复杂的单页应用提供驱动。</p>
<h3 id="Vue的优势"><a href="#Vue的优势" class="headerlink" title="Vue的优势"></a>Vue的优势</h3><p>对于前端开发人员来说，大家使用过很多的Javascript框架。比如原生的JavaScript,Jquery,angularjs和rectjs等。那么Vue与这些框架相比，它的优势体现在哪里？</p>
<ol>
<li>Vue 只关注视图层， 采用自底向上增量开发的设计。Vue关注的是视图层，操作的对象是HTML元素。它是渐进的，没有强主张，你可以在原有大系统的上面，把一两个组件改用它实现，当jQuery用；也可以整个用它全家桶开发，当Angular用；还可以用它的视图，搭配你自己设计的整个下层用。你可以在底层数据逻辑的地方用OO和设计模式的那套理念，也可以函数式，都可以，它只是个轻量视图而已，只做了自己该做的事，没有做不该做的事，仅此而已。</li>
<li>Vue 的目标是通过尽可能简单的 API 实现响应的数据绑定和组合的视图组件。Vue.js 是一个更加灵活开放的解决方案。它允许你以希望的方式组织应用程序，而不是在任何时候都必须遵循 AngularJS 制定的规则，这让 Vue 能适用于各种项目.在 Vue 中指令和组件分得更清晰。指令只封装 DOM 操作，而组件代表一个自给自足的独立单元——有自己的视图和数据逻辑。</li>
<li>Vue 学习起来非常简单.在 API 与设计两方面上 Vue.js非常简单，因此你可以快速地掌握它的全部特性并投入开发。Vue 的整体思想是拥抱经典的 Web 技术，并在其上进行扩展。基于 HTML 的模板使得将已有的应用逐步迁移到 Vue 更为容易。</li>
</ol>
<h3 id="声明式渲染"><a href="#声明式渲染" class="headerlink" title="声明式渲染"></a>声明式渲染</h3><pre><code class="html">&lt;div id=&quot;app&quot;&gt;
  {{ message }}
&lt;/div&gt;</code></pre>
<pre><code class="js">var app = new Vue({
  el: &#39;#app&#39;,
  data: {
    message: &#39;Hello Vue!&#39;
  }
})</code></pre>
<p>如上几行代码就是一个简单的vue程序，你会发现id为app的div里显示了message的值，看起来这跟渲染一个字符串模板非常类似，但是 Vue 在背后做了大量工作。现在数据和 DOM 已经被建立了关联，所有东西都是<strong>响应式的</strong>。我们要怎么确认呢？打开你的浏览器的 JavaScript 控制台 (就在这个页面打开)，并修改 <code>app.message</code> 的值，你将看到上例相应地更新。</p>
<h3 id="指令"><a href="#指令" class="headerlink" title="指令"></a>指令</h3><p>Vue有很多<code>v-</code>开头的指令，其本质是自定义属性。比如<code>v-cloak</code>, <code>v-text</code>, <code>v-bind</code>, <code>v-model</code>, <code>v-on</code>等等，下面将逐个介绍。</p>
<h3 id="v-cloak"><a href="#v-cloak" class="headerlink" title="v-cloak"></a>v-cloak</h3><p>在上面的程序中，如果你的网速太慢，会发现最一开始网页会显示而非它的值，这涉及到Vue的生命周期，我们以后再聊。不过这样一个过程可能给用户带来不好的体验，因此你可以使用<code>v-cloak</code>指令防止页面加载时的闪烁。</p>
<pre><code class="html">&lt;style type=&quot;text/css&quot;&gt;
  /* 
    1、通过属性选择器 选择到 带有属性 v-cloak的标签  让他隐藏
 */
  [v-cloak]{
    /* 元素隐藏    */
    display: none;
  }
  &lt;/style&gt;
&lt;body&gt;
  &lt;div id=&quot;app&quot;&gt;
    &lt;!-- 2、 让带有插值 语法的   添加 v-cloak 属性 
         在 数据渲染完场之后，v-cloak 属性会被自动去除，
         v-cloak一旦移除也就是没有这个属性了  属性选择器就选择不到该标签
         也就是对应的标签会变为可见
    --&gt;
    &lt;div  v-cloak  &gt;{{msg}}&lt;/div&gt;
  &lt;/div&gt;
  &lt;script type=&quot;text/javascript&quot; src=&quot;js/vue.js&quot;&gt;&lt;/script&gt;
  &lt;script type=&quot;text/javascript&quot;&gt;
    var vm = new Vue({
      //  el   指定元素 id 是 app 的元素  
      el: &#39;#app&#39;,
      //  data  里面存储的是数据
      data: {
        msg: &#39;Hello Vue&#39;
      }
    });
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;</code></pre>
<h3 id="v-text"><a href="#v-text" class="headerlink" title="v-text"></a>v-text</h3><ul>
<li><code>v-text</code>指令用于将数据填充到标签中，作用于插值表达式类似，但是没有闪动问题</li>
<li>如果数据中有HTML标签会将html标签一并输出</li>
<li>注意：此处为单向绑定，数据对象上的值改变，插值会发生变化；但是当插值发生变化并不会影响数据对象的值</li>
</ul>
<pre><code class="html">&lt;div id=&quot;app&quot;&gt;
    &lt;!--  
        注意:在指令中不要写插值语法  直接写对应的变量名称 
        在 v-text 中 赋值的时候不要在写 插值语法
        一般属性中不加 {{}}  直接写 对应 的数据名 
    --&gt;
    &lt;p v-text=&quot;msg&quot;&gt;&lt;/p&gt;
    &lt;p&gt;
        &lt;!-- Vue  中只有在标签的 内容中 才用插值语法 --&gt;
        {{msg}}
    &lt;/p&gt;
&lt;/div&gt;

&lt;script&gt;
    new Vue({
        el: &#39;#app&#39;,
        data: {
            msg: &#39;Hello Vue.js&#39;
        }
    });

&lt;/script&gt;</code></pre>
<h3 id="v-html"><a href="#v-html" class="headerlink" title="v-html"></a>v-html</h3><ul>
<li>用法和<code>v-text</code> 相似  但是他可以将HTML片段填充到标签中</li>
<li>可能有安全问题, 一般只在可信任内容上使用 <code>v-html</code>，<strong>永不</strong>用在用户提交的内容上(因为谁也不知道某些傻叉用户会提交啥。。。)</li>
<li>它与v-text区别在于v-text输出的是纯文本，浏览器不会对其再进行html解析，但v-html会将其当html标签解析后输出。</li>
</ul>
<pre><code class="html">&lt;div id=&quot;app&quot;&gt;
　　&lt;p v-html=&quot;html&quot;&gt;&lt;/p&gt; &lt;!-- 输出：html标签在渲染的时候被解析 --&gt;

    &lt;p&gt;{{message}}&lt;/p&gt; &lt;!-- 输出：&lt;span&gt;通过双括号绑定&lt;/span&gt; --&gt;

　　&lt;p v-text=&quot;text&quot;&gt;&lt;/p&gt; &lt;!-- 输出：&lt;span&gt;html标签在渲染的时候被源码输出&lt;/span&gt; --&gt;
&lt;/div&gt;
&lt;script&gt;
　　let app = new Vue({
　　el: &quot;#app&quot;,
　　data: {
　　　　message: &quot;&lt;span&gt;通过双括号绑定&lt;/span&gt;&quot;,
　　　　html: &quot;&lt;span&gt;html标签在渲染的时候被解析&lt;/span&gt;&quot;,
　　　　text: &quot;&lt;span&gt;html标签在渲染的时候被源码输出&lt;/span&gt;&quot;,
　　}
 });
&lt;/script&gt;</code></pre>
<h3 id="v-pre"><a href="#v-pre" class="headerlink" title="v-pre"></a>v-pre</h3><ul>
<li>跳过编译过程显示原始信息</li>
</ul>
<pre><code class="html">&lt;span v-pre&gt;{{ this will not be compiled }}&lt;/span&gt;    
&lt;!--  显示的是{{ this will not be compiled }}  --&gt;
&lt;span v-pre&gt;{{msg}}&lt;/span&gt;  
&lt;!--   即使data里面定义了msg这里仍然是显示的{{msg}}  --&gt;
&lt;script&gt;
    new Vue({
        el: &#39;#app&#39;,
        data: {
            msg: &#39;Hello Vue.js&#39;
        }
    });

&lt;/script&gt;</code></pre>
<h3 id="v-once"><a href="#v-once" class="headerlink" title="v-once"></a>v-once</h3><ul>
<li>只渲染一次，当数据改变时，插值的内容不会有响应式的改变</li>
</ul>
<pre><code class="html">  &lt;!-- 即使data里面定义了msg 后期我们修改了 仍然显示的是第一次data里面存储的数据即 Hello Vue.js  --&gt;
     &lt;span v-once&gt;{{ msg}}&lt;/span&gt;    
&lt;script&gt;
    new Vue({
        el: &#39;#app&#39;,
        data: {
            msg: &#39;Hello Vue.js&#39;
        }
    });
&lt;/script&gt;</code></pre>
<h3 id="v-on"><a href="#v-on" class="headerlink" title="v-on"></a>v-on</h3><ul>
<li><code>v-on</code>是用来绑定事件的指令，如：v-on:click  </li>
<li>该指令还有一个语法糖，上述指令可以缩写为：@click</li>
</ul>
<pre><code class="html">&lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;
        &lt;div&gt;{{num}}&lt;/div&gt;
    &lt;div&gt;
        &lt;button v-on:click=&quot;num++&quot;&gt;点击1&lt;/button&gt;
        &lt;button @click=&quot;num++&quot;&gt;点击2&lt;/button&gt;
        &lt;button @click=&#39;handle&#39;&gt;点击3&lt;/button&gt;
        &lt;button @click=&#39;handle()&#39;&gt;点击4&lt;/button&gt;
    &lt;/div&gt;
    &lt;/div&gt;
    &lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
    &lt;script&gt;
        var vm = new Vue({
            el: &#39;#app&#39;,
            data: {
                num: 0
            },
            methods: {
                handle: function(){
                    this.num ++;
                }
            },
        })
    &lt;/script&gt;
&lt;/body&gt;</code></pre>
<p>上面代码中四个按钮都可以实现对num加一 的操作，但是按钮3和4值得注意一下，按钮3在调用方法时没有加括号而按钮4加了括号，两种方式都是可以的。原因在于如果我们的方法不需要参数，在调用时就无须括号。</p>
<blockquote>
<p>实际上，如果方法内只需要一个event事件对象参数，我们在调用时也无须写括号，因为vue会帮助我们自动传入event对象，前提是方法的声明处只有一个参数。如果方法需要多个参数而我们没有传入，则默认传入event作为第一个参数，其他的会是undefined。</p>
</blockquote>
<h4 id="事件修饰符"><a href="#事件修饰符" class="headerlink" title="事件修饰符"></a>事件修饰符</h4><ul>
<li>在事件处理程序中调用 <code>event.preventDefault()</code> 或 <code>event.stopPropagation()</code> 是非常常见的需求。</li>
<li>Vue 不推荐我们操作DOM    为了解决这个问题，Vue.js 为 <code>v-on</code> 提供了<strong>事件修饰符</strong></li>
<li>修饰符是由点开头的指令后缀来表示的</li>
</ul>
<pre><code class="html">&lt;!-- 阻止单击事件继续传播 --&gt;
&lt;a v-on:click.stop=&quot;doThis&quot;&gt;&lt;/a&gt;

&lt;!-- 提交事件不再重载页面 --&gt;
&lt;form v-on:submit.prevent=&quot;onSubmit&quot;&gt;&lt;/form&gt;

&lt;!-- 修饰符可以串联   即阻止冒泡也阻止默认事件 --&gt;
&lt;a v-on:click.stop.prevent=&quot;doThat&quot;&gt;&lt;/a&gt;

&lt;!-- 只当在 event.target 是当前元素自身时触发处理函数 --&gt;
&lt;!-- 即事件不是从内部元素触发的 --&gt;
&lt;div v-on:click.self=&quot;doThat&quot;&gt;...&lt;/div&gt;

使用修饰符时，顺序很重要；相应的代码会以同样的顺序产生。因此，用 v-on:click.prevent.self 会阻止所有的点击，而 v-on:click.self.prevent 只会阻止对元素自身的点击。</code></pre>
<h4 id="按键修饰符"><a href="#按键修饰符" class="headerlink" title="按键修饰符"></a>按键修饰符</h4><ul>
<li>在做项目中有时会用到键盘事件，在监听键盘事件时，我们经常需要检查详细的按键。Vue 允许为 <code>v-on</code> 在监听键盘事件时添加按键修饰符</li>
</ul>
<pre><code class="html">&lt;!-- 只有在 `keyCode` 是 13 时调用 `vm.submit()` --&gt;
&lt;input v-on:keyup.13=&quot;submit&quot;&gt;

&lt;!-- -当点击enter 时调用 `vm.submit()` --&gt;
&lt;input v-on:keyup.enter=&quot;submit&quot;&gt;

&lt;!--当点击enter或者space时  时调用 `vm.alertMe()`   --&gt;
&lt;input type=&quot;text&quot; v-on:keyup.enter.space=&quot;alertMe&quot; &gt;

常用的按键修饰符
.enter =&gt;    enter键
.tab =&gt; tab键
.delete (捕获“删除”和“退格”按键) =&gt;  删除键
.esc =&gt; 取消键
.space =&gt;  空格键
.up =&gt;  上
.down =&gt;  下
.left =&gt;  左
.right =&gt;  右

&lt;script&gt;
    var vm = new Vue({
        el:&quot;#app&quot;,
        methods: {
              submit:function(){},
              alertMe:function(){},
        }
    })

&lt;/script&gt;</code></pre>
<h4 id="自定义按键修饰符"><a href="#自定义按键修饰符" class="headerlink" title="自定义按键修饰符"></a>自定义按键修饰符</h4><ul>
<li>在Vue中可以通过<code>config.keyCodes</code>自定义按键修饰符别名</li>
</ul>
<pre><code class="html">&lt;div id=&quot;app&quot;&gt;
    预先定义了keycode 116（即F5）的别名为f5，因此在文字输入框中按下F5，会触发prompt方法
    &lt;input type=&quot;text&quot; v-on:keydown.f5=&quot;prompt()&quot;&gt;
&lt;/div&gt;

&lt;script&gt;

    Vue.config.keyCodes.f5 = 116;

    let app = new Vue({
        el: &#39;#app&#39;,
        methods: {
            prompt: function() {
                alert(&#39;我是 F5！&#39;);
            }
        }
    });
&lt;/script&gt;</code></pre>
<h3 id="v-bind"><a href="#v-bind" class="headerlink" title="v-bind"></a>v-bind</h3><ul>
<li>v-bind 指令被用来响应地更新 HTML 属性</li>
<li>v-bind:href    可以缩写为    :href;</li>
</ul>
<pre><code class="html">&lt;!-- 绑定一个属性 --&gt;
&lt;img v-bind:src=&quot;imageSrc&quot;&gt;

&lt;!-- 缩写 --&gt;
&lt;img :src=&quot;imageSrc&quot;&gt;</code></pre>
<h4 id="绑定对象"><a href="#绑定对象" class="headerlink" title="绑定对象"></a>绑定对象</h4><ul>
<li>我们可以给v-bind:class 一个对象，以动态地切换class。</li>
<li>注意：v-bind:class指令可以与普通的class特性共存</li>
</ul>
<pre><code class="html">1、 v-bind 中支持绑定一个对象 
    如果绑定的是一个对象 则 键为 对应的类名  值 为对应data中的数据 
&lt;!-- 
    HTML最终渲染为 &lt;ul class=&quot;box textColor textSize&quot;&gt;&lt;/ul&gt;
    注意：
        textColor，textSize  对应的渲染到页面上的CSS类名    
        isColor，isSize  对应vue data中的数据  如果为true 则对应的类名 渲染到页面上 


        当 isColor 和 isSize 变化时，class列表将相应的更新，
        例如，将isSize改成false，
        class列表将变为 &lt;ul class=&quot;box textColor&quot;&gt;&lt;/ul&gt;
--&gt;

&lt;ul class=&quot;box&quot; v-bind:class=&quot;{textColor:isColor, textSize:isSize}&quot;&gt;
    &lt;li&gt;学习Vue&lt;/li&gt;
    &lt;li&gt;学习Node&lt;/li&gt;
    &lt;li&gt;学习React&lt;/li&gt;
&lt;/ul&gt;
  &lt;div v-bind:style=&quot;{color:activeColor,fontSize:activeSize}&quot;&gt;对象语法&lt;/div&gt;

&lt;sript&gt;
var vm= new Vue({
    el:&#39;.box&#39;,
    data:{
        isColor:true,
        isSize:true，
        activeColor:&quot;red&quot;,
        activeSize:&quot;25px&quot;,
    }
})
&lt;/sript&gt;
&lt;style&gt;

    .box{
        border:1px dashed #f0f;
    }
    .textColor{
        color:#f00;
        background-color:#eef;
    }
    .textSize{
        font-size:30px;
        font-weight:bold;
    }
&lt;/style&gt;</code></pre>
<h4 id="绑定class"><a href="#绑定class" class="headerlink" title="绑定class"></a>绑定class</h4><pre><code class="html">2、  v-bind 中支持绑定一个数组    数组中classA和 classB 对应为data中的数据

这里的classA  对用data 中的  classA
这里的classB  对用data 中的  classB
&lt;ul class=&quot;box&quot; :class=&quot;[classA, classB]&quot;&gt;
    &lt;li&gt;学习Vue&lt;/li&gt;
    &lt;li&gt;学习Node&lt;/li&gt;
    &lt;li&gt;学习React&lt;/li&gt;
&lt;/ul&gt;
&lt;script&gt;
var vm= new Vue({
    el:&#39;.box&#39;,
    data:{
        classA:‘textColor‘,
        classB:‘textSize‘
    }
})
&lt;/script&gt;
&lt;style&gt;
    .box{
        border:1px dashed #f0f;
    }
    .textColor{
        color:#f00;
        background-color:#eef;
    }
    .textSize{
        font-size:30px;
        font-weight:bold;
    }
&lt;/style&gt;</code></pre>
<h4 id="绑定对象和数组的区别"><a href="#绑定对象和数组的区别" class="headerlink" title="绑定对象和数组的区别"></a>绑定对象和数组的区别</h4><ul>
<li>绑定对象的时候 对象的属性 即要渲染的类名 对象的属性值对应的是 data 中的数据 </li>
<li>绑定数组的时候数组里面存的是data 中的数据 </li>
</ul>
<h4 id="绑定style"><a href="#绑定style" class="headerlink" title="绑定style"></a>绑定style</h4><pre><code class="html">&lt;div v-bind:style=&quot;styleObject&quot;&gt;绑定样式对象&lt;/div&gt;&#39;

&lt;!-- CSS 属性名可以用驼峰式 (camelCase) 或短横线分隔 (kebab-case，记得用单引号括起来)    --&gt;
 &lt;div v-bind:style=&quot;{ color: activeColor, fontSize: fontSize,background:&#39;red&#39; }&quot;&gt;内联样式&lt;/div&gt;

&lt;!--组语法可以将多个样式对象应用到同一个元素 --&gt;
&lt;div v-bind:style=&quot;[styleObj1, styleObj2]&quot;&gt;&lt;/div&gt;


&lt;script&gt;
    new Vue({
      el: &#39;#app&#39;,
      data: {
        styleObject: {
          color: &#39;green&#39;,
          fontSize: &#39;30px&#39;,
          background:&#39;red&#39;
        }，
        activeColor: &#39;green&#39;,
           fontSize: &quot;30px&quot;
      },
      styleObj1: {
             color: &#39;red&#39;
       },
       styleObj2: {
            fontSize: &#39;30px&#39;
       }

&lt;/script&gt;</code></pre>
<h3 id="分支结构"><a href="#分支结构" class="headerlink" title="分支结构"></a>分支结构</h3><h4 id="v-if-使用场景"><a href="#v-if-使用场景" class="headerlink" title="v-if 使用场景"></a>v-if 使用场景</h4><ul>
<li>多个元素 通过条件判断展示或者隐藏某个元素。或者多个元素</li>
<li>进行两个视图之间的切换</li>
</ul>
<pre><code class="html">&lt;div id=&quot;app&quot;&gt;
        &lt;!--  判断是否加载，如果为真，就加载，否则不加载--&gt;
        &lt;span v-if=&quot;flag&quot;&gt;
           如果flag为true则显示,false不显示!
        &lt;/span&gt;
&lt;/div&gt;

&lt;script&gt;
    var vm = new Vue({
        el:&quot;#app&quot;,
        data:{
            flag:true
        }
    })
&lt;/script&gt;

----------------------------------------------------------

    &lt;div v-if=&quot;type === &#39;A&#39;&quot;&gt;
       A
    &lt;/div&gt;
  &lt;!-- v-else-if紧跟在v-if或v-else-if之后   表示v-if条件不成立时执行--&gt;
    &lt;div v-else-if=&quot;type === &#39;B&#39;&quot;&gt;
       B
    &lt;/div&gt;
    &lt;div v-else-if=&quot;type === &#39;C&#39;&quot;&gt;
       C
    &lt;/div&gt;
  &lt;!-- v-else紧跟在v-if或v-else-if之后--&gt;
    &lt;div v-else&gt;
       Not A/B/C
    &lt;/div&gt;

&lt;script&gt;
    new Vue({
      el: &#39;#app&#39;,
      data: {
        type: &#39;C&#39;
      }
    })
&lt;/script&gt;</code></pre>
<h4 id="v-show-和-v-if的区别"><a href="#v-show-和-v-if的区别" class="headerlink" title="v-show 和 v-if的区别"></a>v-show 和 v-if的区别</h4><ul>
<li>v-show本质就是标签display设置为none，控制隐藏<ul>
<li>v-show只编译一次，后面其实就是控制css，而v-if不停的销毁和创建，故v-show性能更好一点。</li>
</ul>
</li>
<li>v-if是动态的向DOM树内添加或者删除DOM元素<ul>
<li>v-if切换有一个局部编译/卸载的过程，切换过程中合适地销毁和重建内部的事件监听和子组件</li>
</ul>
</li>
</ul>
<h3 id="循环结构"><a href="#循环结构" class="headerlink" title="循环结构"></a>循环结构</h3><h4 id="v-for"><a href="#v-for" class="headerlink" title="v-for"></a>v-for</h4><ul>
<li>用于循环的数组里面的值可以是对象，也可以是普通元素</li>
</ul>
<pre><code class="html">&lt;ul id=&quot;example-1&quot;&gt;
   &lt;!-- 循环结构-遍历数组  
    item 是我们自己定义的一个名字  代表数组里面的每一项  
    items对应的是 data中的数组--&gt;
  &lt;li v-for=&quot;item in items&quot;&gt;
    {{ item.message }}
  &lt;/li&gt; 

&lt;/ul&gt;
&lt;script&gt;
 new Vue({
  el: &#39;#example-1&#39;,
  data: {
    items: [
      { message: &#39;Foo&#39; },
      { message: &#39;Bar&#39; }
    ]，

  }
})
&lt;/script&gt;</code></pre>
<ul>
<li><strong>不推荐</strong>同时使用 <code>v-if</code> 和 <code>v-for</code></li>
<li>当 <code>v-if</code> 与 <code>v-for</code> 一起使用时，<code>v-for</code> 具有比 <code>v-if</code> 更高的优先级。</li>
</ul>
<pre><code class="html">   &lt;!--  循环结构-遍历对象
        v 代表   对象的value
        k  代表对象的 键 
        i  代表索引    
    ---&gt; 
     &lt;div v-if=&#39;v==13&#39; v-for=&#39;(v,k,i) in obj&#39;&gt;{{v + '---' + k + '---' + i}}&lt;/div&gt;

&lt;script&gt;
 new Vue({
  el: &#39;#example-1&#39;,
  data: {
    items: [
      { message: &#39;Foo&#39; },
      { message: &#39;Bar&#39; }
    ]，
    obj: {
        uname: &#39;zhangsan&#39;,
        age: 13,
        gender: &#39;female&#39;
    }
  }
})
&lt;/script&gt;</code></pre>
<p>key 的作用</p>
<ul>
<li><strong>key来给每个节点做一个唯一标识</strong></li>
<li><strong>key的作用主要是为了高效的更新虚拟DOM</strong></li>
</ul>
<pre><code class="html">&lt;ul&gt;
  &lt;li v-for=&quot;item in items&quot; :key=&quot;item.id&quot;&gt;...&lt;/li&gt;
&lt;/ul&gt;</code></pre>
<h3 id="购物车案例"><a href="#购物车案例" class="headerlink" title="购物车案例"></a>购物车案例</h3><p><strong>需求说明：</strong></p>
<ul>
<li>表格显示购物车的商品内容</li>
<li>可以增加或者减少物品的数量，并且可以删除</li>
<li>显示购物车商品的总价</li>
</ul>
<p><strong>实现图：</strong></p>
<p><img src="http://image.cheerway.fun/blog/20200304/bXC4dppdTWJQ.png?imageslim" alt="mark"></p>
<p><strong>代码:</strong></p>
<pre><code class="html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
  &lt;meta charset=&quot;UTF-8&quot;&gt;
  &lt;title&gt;Title&lt;/title&gt;
  &lt;link rel=&quot;stylesheet&quot; href=&quot;https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css&quot; integrity=&quot;sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T&quot; crossorigin=&quot;anonymous&quot;&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;app&quot; class=&quot;container&quot;&gt;
  &lt;table class=&quot;table table-hover table-bordered&quot;&gt;
    &lt;thead&gt;
    &lt;tr&gt;
      &lt;th scope=&quot;col&quot;&gt;ID&lt;/th&gt;
      &lt;th scope=&quot;col&quot;&gt;书籍名称&lt;/th&gt;
      &lt;th scope=&quot;col&quot;&gt;出版日期&lt;/th&gt;
      &lt;th scope=&quot;col&quot;&gt;价格&lt;/th&gt;
      &lt;th scope=&quot;col&quot;&gt;购买数量&lt;/th&gt;
      &lt;th scope=&quot;col&quot;&gt;操作&lt;/th&gt;
    &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
    &lt;tr v-for=&quot;(book,index) in books&quot;&gt;
      &lt;td&gt;{{index+1}}&lt;/td&gt;
      &lt;td&gt;{{book.name}}&lt;/td&gt;
      &lt;td&gt;{{book.date}}&lt;/td&gt;
      &lt;td&gt;{{book.price | showPrice}}&lt;/td&gt;
      &lt;td&gt;
        &lt;button class=&quot;btn btn-sm btn-secondary&quot; @click=&quot;decrement(index)&quot;&gt;-&lt;/button&gt;
        {{book.count}}
        &lt;button class=&quot;btn btn-sm btn-secondary&quot; @click=&quot;increment(index)&quot;&gt;+&lt;/button&gt;
      &lt;/td&gt;
      &lt;td&gt;
        &lt;button class=&quot;btn btn-sm btn-danger&quot; @click=&quot;removeItem(index)&quot;&gt;删除&lt;/button&gt;
      &lt;/td&gt;
    &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
  &lt;span&gt;总价格:{{totalPrice | showPrice}}&lt;/span&gt;
&lt;/div&gt;
&lt;script src=&quot;https://code.jquery.com/jquery-3.3.1.slim.min.js&quot; integrity=&quot;sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo&quot; crossorigin=&quot;anonymous&quot;&gt;&lt;/script&gt;
&lt;script src=&quot;https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js&quot; integrity=&quot;sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1&quot; crossorigin=&quot;anonymous&quot;&gt;&lt;/script&gt;
&lt;script src=&quot;https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js&quot; integrity=&quot;sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM&quot; crossorigin=&quot;anonymous&quot;&gt;&lt;/script&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue/dist/vue.js&quot;&gt;&lt;/script&gt;
&lt;script&gt;
  const app = new Vue({
    el: &quot;#app&quot;,
    data: {
      books: [
        {
          name: &#39;《算法导论》&#39;,
          date: &#39;2016-9&#39;,
          price: 85,
          count: &#39;1&#39;
        },
        {
          name: &#39;《UNIX编程艺术》&#39;,
          date: &#39;2016-2&#39;,
          price: 99,
          count: &#39;1&#39;
        },
        {
          name: &#39;《编程珠玑》&#39;,
          date: &#39;2008-10&#39;,
          price: 108,
          count: &#39;1&#39;
        },
        {
          name: &#39;《代码大全》&#39;,
          date: &#39;2006-3&#39;,
          price: 79,
          count: &#39;1&#39;
        }
      ],
    },
    filters: {
      showPrice(value){
        return &#39;￥&#39; + value.toFixed(2);
      }
    },
    computed: {
      totalPrice(){
        let total = 0;
        for (let item of this.books){
          total += item.price * item.count;
        }
        return total;
      }
    },
    methods: {
      increment(index){
        this.books[index].count++;
      },
      decrement(index){
        if (this.books[index].count &gt; 0){
          this.books[index].count--;
        }
      },
      removeItem(index){
        this.books.splice(index,1);
      }
    }
  });
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;</code></pre>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>笔记</tag>
        <tag>Vue</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 日志全解</title>
    <url>/2020/ck74nrh5r0000xktndqn5h6i6/</url>
    <content><![CDATA[<h2 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h2><h3 id="1、日志框架介绍"><a href="#1、日志框架介绍" class="headerlink" title="1、日志框架介绍"></a>1、日志框架介绍</h3><p><strong>主流的日志框架：</strong></p>
<p>JUL、JCL、Jboss-logging、logback、log4j、log4j2、slf4j….</p>
<table>
<thead>
<tr>
<th align="center">日志门面  （日志的抽象层）</th>
<th align="center">日志实现</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><del>JCL（Jakarta  Commons Logging）</del>    SLF4j（Simple  Logging Facade for Java）    <strong><del>jboss-logging</del></strong></td>
<td align="center">Log4j  JUL（java.util.logging）  Log4j2  <strong>Logback</strong></td>
</tr>
</tbody></table>
<p>一般来说在开发中在左边选择一个抽象层，右边选择一个实现。</p>
<p>SpringBoot默认选用<code>SLF4j</code>和<code>logback</code>，SpringBoot的底层是Spring，而Spring默认是JCL。</p>
<h3 id="2、SLF4J的使用"><a href="#2、SLF4J的使用" class="headerlink" title="2、SLF4J的使用"></a>2、SLF4J的使用</h3><h4 id="如何在系统中使用SLF4J？"><a href="#如何在系统中使用SLF4J？" class="headerlink" title="如何在系统中使用SLF4J？"></a>如何在系统中使用SLF4J？</h4><p>可以参考：<a href="https://www.slf4j.org" target="_blank" rel="noopener">https://www.slf4j.org</a></p>
<p>开发时候日志记录方法的调用，不应该来直接调用日志的实现类，而是调用日志抽象层里面的方法。</p>
<p>给系统里导入slf4j的jar和  logback的实现jar，就可以进行如下示例：</p>
<pre><code class="java">import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HelloWorld {
  public static void main(String[] args) {
    Logger logger = LoggerFactory.getLogger(HelloWorld.class);
    logger.info(&quot;Hello World&quot;);
  }
}</code></pre>
<p><img src="http://image.cheerway.fun/blog/20200227/HrCXCAHqbOx7.png?imageslim" alt="mark"></p>
<p>每一个日志的实现框架都有自己的配置文件。使用slf4j以后，<strong>配置文件还是做成日志实现框架自己本身的配置文件；</strong></p>
<h4 id="遗留问题"><a href="#遗留问题" class="headerlink" title="遗留问题"></a>遗留问题</h4><p>下面是几种框架的日志选择：</p>
<p>SpringBoot（slf4j+logback）: Spring（commons-logging）、Hibernate（jboss-logging）、MyBatis、xxxx</p>
<p>统一日志记录，即使是别的框架和我一起统一使用slf4j进行输出？</p>
<p><img src="http://image.cheerway.fun/blog/20200227/YqDpaS53HU61.png?imageslim" alt="mark"></p>
<p><strong>如何让系统中所有的日志都统一到slf4j；</strong></p>
<p>1、将系统中其他日志框架先排除出去；</p>
<p>2、用中间包来替换原有的日志框架；</p>
<p>3、我们导入slf4j其他的实现</p>
<h3 id="3、SpringBoot的日志关系"><a href="#3、SpringBoot的日志关系" class="headerlink" title="3、SpringBoot的日志关系"></a>3、SpringBoot的日志关系</h3><p>SpringBoot的日志starter是：</p>
<pre><code class="xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-logging&lt;/artifactId&gt;
&lt;/dependency&gt;</code></pre>
<p>底层的依赖关系：</p>
<p><img src="http://image.cheerway.fun/blog/20200227/3P4RoG9Cgt3Q.png?imageslim" alt="mark"></p>
<p>总结：</p>
<p>​    1）、SpringBoot底层也是使用slf4j+logback的方式进行日志记录</p>
<p>​    2）、SpringBoot也把其他的日志都替换成了slf4j；</p>
<p>​    3）、中间替换包？</p>
<pre><code class="java">@SuppressWarnings(&quot;rawtypes&quot;)
public abstract class LogFactory {

    static String UNSUPPORTED_OPERATION_IN_JCL_OVER_SLF4J = &quot;http://www.slf4j.org/codes.html#unsupported_operation_in_jcl_over_slf4j&quot;;

    static LogFactory logFactory = new SLF4JLogFactory();</code></pre>
<p><img src="http://image.cheerway.fun/blog/20200227/dYYSl4SCYGyB.png?imageslim" alt="mark"></p>
<p>4）、如果我们要引入其他框架？一定要把这个框架的默认日志依赖移除掉？</p>
<p>Spring框架用的是commons-logging；</p>
<pre><code class="xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework&lt;/groupId&gt;
    &lt;artifactId&gt;spring-core&lt;/artifactId&gt;
    &lt;exclusions&gt;
        &lt;exclusion&gt;
            &lt;groupId&gt;commons-logging&lt;/groupId&gt;
            &lt;artifactId&gt;commons-logging&lt;/artifactId&gt;
        &lt;/exclusion&gt;
    &lt;/exclusions&gt;
&lt;/dependency&gt;</code></pre>
<p><strong>SpringBoot能自动适配所有的日志，而且底层使用slf4j+logback的方式记录日志，引入其他框架的时候，只需要把这个框架依赖的日志框架排除掉即可；</strong></p>
<h3 id="4、日志使用"><a href="#4、日志使用" class="headerlink" title="4、日志使用"></a>4、日志使用</h3><h4 id="默认配置"><a href="#默认配置" class="headerlink" title="默认配置"></a>默认配置</h4><p>SpringBoot默认帮我们配置好了日志；</p>
<pre><code class="java">//记录器
Logger logger = LoggerFactory.getLogger(getClass());
@Test
public void contextLoads() {
    //System.out.println();

    //日志的级别；
    //由低到高   trace&lt;debug&lt;info&lt;warn&lt;error
    //可以调整输出的日志级别；日志就只会在这个级别以以后的高级别生效
    logger.trace(&quot;这是trace日志...&quot;);
    logger.debug(&quot;这是debug日志...&quot;);
    //SpringBoot默认给我们使用的是info级别的，没有指定级别的就用SpringBoot默认规定的级别；root级别
    logger.info(&quot;这是info日志...&quot;);
    logger.warn(&quot;这是warn日志...&quot;);
    logger.error(&quot;这是error日志...&quot;);


}</code></pre>
<p>日志输出格式：</p>
<blockquote>
<pre><code>    %d表示日期时间，
    %thread表示线程名，
    %-5level：级别从左显示5个字符宽度
    %logger{50} 表示logger名字最长50个字符，否则按照句点分割。 
    %msg：日志消息，
    %n是换行符
--&gt;
%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</code></pre></blockquote>
<p>SpringBoot修改日志的默认配置</p>
<pre><code class="properties">logging.level.com.atguigu=trace


#logging.path=
# 不指定路径在当前项目下生成springboot.log日志
# 可以指定完整的路径；
#logging.file=G:/springboot.log

# 在当前磁盘的根路径下创建spring文件夹和里面的log文件夹；使用 spring.log 作为默认文件
logging.path=/spring/log

#  在控制台输出的日志的格式
logging.pattern.console=%d{yyyy-MM-dd} [%thread] %-5level %logger{50} - %msg%n
# 指定文件中日志输出的格式
logging.pattern.file=%d{yyyy-MM-dd} === [%thread] === %-5level === %logger{50} ==== %msg%n</code></pre>
<table>
<thead>
<tr>
<th>logging.file</th>
<th>logging.path</th>
<th>Example</th>
<th>Description</th>
</tr>
</thead>
<tbody><tr>
<td>(none)</td>
<td>(none)</td>
<td></td>
<td>只在控制台输出</td>
</tr>
<tr>
<td>指定文件名</td>
<td>(none)</td>
<td>my.log</td>
<td>输出日志到my.log文件</td>
</tr>
<tr>
<td>(none)</td>
<td>指定目录</td>
<td>/var/log</td>
<td>输出到指定目录的 spring.log 文件中</td>
</tr>
</tbody></table>
<h4 id="指定配置"><a href="#指定配置" class="headerlink" title="指定配置"></a>指定配置</h4><p>给类路径下放上每个日志框架自己的配置文件即可；SpringBoot就不使用他默认配置的了</p>
<table>
<thead>
<tr>
<th>Logging System</th>
<th>Customization</th>
</tr>
</thead>
<tbody><tr>
<td>Logback</td>
<td><code>logback-spring.xml</code>, <code>logback-spring.groovy</code>, <code>logback.xml</code> or <code>logback.groovy</code></td>
</tr>
<tr>
<td>Log4j2</td>
<td><code>log4j2-spring.xml</code> or <code>log4j2.xml</code></td>
</tr>
<tr>
<td>JDK (Java Util Logging)</td>
<td><code>logging.properties</code></td>
</tr>
</tbody></table>
<p>logback.xml：直接就被日志框架识别了；</p>
<p><strong>logback-spring.xml</strong>：日志框架就不直接加载日志的配置项，由SpringBoot解析日志配置，可以使用SpringBoot的高级Profile功能</p>
<pre><code class="xml">&lt;springProfile name=&quot;staging&quot;&gt;
    &lt;!-- configuration to be enabled when the &quot;staging&quot; profile is active --&gt;
      可以指定某段配置只在某个环境下生效
&lt;/springProfile&gt;
</code></pre>
<p>如：</p>
<pre><code class="xml">&lt;appender name=&quot;stdout&quot; class=&quot;ch.qos.logback.core.ConsoleAppender&quot;&gt;
    &lt;!--
        日志输出格式：
   %d表示日期时间，
   %thread表示线程名，
   %-5level：级别从左显示5个字符宽度
   %logger{50} 表示logger名字最长50个字符，否则按照句点分割。 
   %msg：日志消息，
   %n是换行符
        --&gt;
    &lt;layout class=&quot;ch.qos.logback.classic.PatternLayout&quot;&gt;
        &lt;springProfile name=&quot;dev&quot;&gt;
            &lt;pattern&gt;%d{yyyy-MM-dd HH:mm:ss.SSS} ----&gt; [%thread] ---&gt; %-5level %logger{50} - %msg%n&lt;/pattern&gt;
        &lt;/springProfile&gt;
        &lt;springProfile name=&quot;!dev&quot;&gt;
            &lt;pattern&gt;%d{yyyy-MM-dd HH:mm:ss.SSS} ==== [%thread] ==== %-5level %logger{50} - %msg%n&lt;/pattern&gt;
        &lt;/springProfile&gt;
    &lt;/layout&gt;
&lt;/appender&gt;</code></pre>
<p>如果使用logback.xml作为日志配置文件，还要使用profile功能，会有以下错误</p>
<p> <code>no applicable action for [springProfile]</code></p>
<h3 id="5、切换日志框架"><a href="#5、切换日志框架" class="headerlink" title="5、切换日志框架"></a>5、切换日志框架</h3><p>可以按照slf4j的日志适配图，进行相关的切换；</p>
<p><strong>slf4j+log4j的方式：</strong></p>
<pre><code class="xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
    &lt;exclusions&gt;
        &lt;exclusion&gt;
            &lt;artifactId&gt;logback-classic&lt;/artifactId&gt;
            &lt;groupId&gt;ch.qos.logback&lt;/groupId&gt;
        &lt;/exclusion&gt;
        &lt;exclusion&gt;
            &lt;artifactId&gt;log4j-over-slf4j&lt;/artifactId&gt;
            &lt;groupId&gt;org.slf4j&lt;/groupId&gt;
        &lt;/exclusion&gt;
    &lt;/exclusions&gt;
&lt;/dependency&gt;

&lt;dependency&gt;
    &lt;groupId&gt;org.slf4j&lt;/groupId&gt;
    &lt;artifactId&gt;slf4j-log4j12&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<p><strong>切换为log4j2：</strong></p>
<pre><code class="xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
    &lt;exclusions&gt;
        &lt;exclusion&gt;
            &lt;artifactId&gt;spring-boot-starter-logging&lt;/artifactId&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
        &lt;/exclusion&gt;
    &lt;/exclusions&gt;
&lt;/dependency&gt;

&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-log4j2&lt;/artifactId&gt;
&lt;/dependency&gt;</code></pre>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 配置文件详解（告别XML）</title>
    <url>/2020/ck735cvux0000xktn68oo02qa/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><a href="https://www.cheerway.fun/2020/ck71sexg50000cstn3hy6dnen/">上篇笔记</a>中提到SpringBoot简化Spring的开发是通过大量的默认配置，然而在开发中我们有时需要改变这些默认约定，所以要使用配置文件。</p>
<h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><p>SpringBoot中的配置文件明必须是application，如：<code>application.yml</code>，<code>application.properties</code>，<code>application.yaml</code></p>
<p>配置文件的作用: 帮我们修改自动配置的默认值;</p>
<h2 id="YAML"><a href="#YAML" class="headerlink" title="YAML"></a>YAML</h2><p>YAML 语言（发音 /ˈjæməl/ ）的设计目标，就是方便人类读写。它实质上是一种通用的数据串行化格式。</p>
<p>它的基本语法如下:</p>
<blockquote>
<ul>
<li>大小写敏感</li>
<li>使用缩进表示层级关系</li>
<li>缩进时不允许使用Tab键，只允许使用空格。</li>
<li>缩进的空格数目不重要，只要相同层级的元素左侧对齐即可</li>
<li># 表示注释, 作用一直到行尾</li>
</ul>
</blockquote>
<p>YAML支持的数据结构:</p>
<blockquote>
<ul>
<li>对象：键值对的集合，又称为映射（mapping）/ 哈希（hashes） / 字典（dictionary）</li>
<li>数组：一组按次序排列的值，又称为序列（sequence） / 列表（list）</li>
<li>纯量（scalars）：单个的、不可再分的值</li>
</ul>
</blockquote>
<h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><h4 id="纯量-字面量"><a href="#纯量-字面量" class="headerlink" title="纯量(字面量)"></a>纯量(字面量)</h4><p>所谓的字面量就是指字符串，数字，布尔值等基本数据；</p>
<p>字符串默认不需要单引号和双引号，如果你非要用的话，请看下面的规则：</p>
<p><code>“”</code>：双引号，不会转义字符串里面的特殊字符；特殊字符会作为本身想表示的意思</p>
<blockquote>
<p>举个栗子：name: “zhangsan \n lisi”：输出；zhangsan 换行 lisi</p>
</blockquote>
<p><code>‘’</code>：单引号，会转义特殊字符，特殊字符最终只是一个普通的字符串数据</p>
<blockquote>
<p>举个栗子：name: ‘zhangsan \n lisi’：输出；zhangsan \n lisi</p>
</blockquote>
<h4 id="对象（键值对的集合）"><a href="#对象（键值对的集合）" class="headerlink" title="对象（键值对的集合）"></a>对象（键值对的集合）</h4><p><code>k: v</code>：对象的一组键值对，使用冒号结构表示，冒号后必须有空格</p>
<pre><code class="yaml">pet:
    name:  cat
    age:  2
    sex:  公的</code></pre>
<p>如果你愿意的话，也可以把它写成一行：</p>
<pre><code class="yaml">pet:  {name: cat,gender: 公的,age: 22}</code></pre>
<h4 id="数组，List集合"><a href="#数组，List集合" class="headerlink" title="数组，List集合"></a>数组，List集合</h4><p>基本写法</p>
<pre><code class="yaml">list:
    - item1
    - item2
    - item3</code></pre>
<p>写成一行的话就是：</p>
<pre><code class="yaml">list: [item1,item2,item3]</code></pre>
<h2 id="开发实例"><a href="#开发实例" class="headerlink" title="开发实例"></a>开发实例</h2><h3 id="导入坐标"><a href="#导入坐标" class="headerlink" title="导入坐标"></a>导入坐标</h3><p>在pom.xml中加入如下坐标：</p>
<pre><code class="xml">&lt;!--导入配置文件处理器，配置文件进行绑定就会有提示--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-configuration-processor&lt;/artifactId&gt;
            &lt;optional&gt;true&lt;/optional&gt;
        &lt;/dependency&gt;</code></pre>
<h3 id="编写实体类："><a href="#编写实体类：" class="headerlink" title="编写实体类："></a>编写实体类：</h3><pre><code class="java">@Component
@ConfigurationProperties(prefix = &quot;person&quot;)
public class Person {

    private String userName;
    private Integer age;
    private Date birthday;
    private Boolean sex;
    private Map&lt;String,Object&gt; map;
    private List&lt;Object&gt; list;
    private Cat cat;

    getter，setter，toString方法此处省略......

}</code></pre>
<pre><code class="java">@Component
public class Cat {

    private String name;
    private Integer age;

    getter，setter，toString方法此处省略......

}</code></pre>
<h3 id="配置文件-1"><a href="#配置文件-1" class="headerlink" title="配置文件"></a>配置文件</h3><pre><code class="yaml">person:
  username: 张三
  age: 18
  birthday: 2020/02/26
  sex: false
  map: {k1: v1,k2: 22}
  list:
    - item1
    - item2
    - item3
  cat:
    name: 猫咪
    age: 2</code></pre>
<h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><pre><code class="java">@SpringBootTest
class Day02SettingsApplicationTests {

    @Autowired
    Person person;

    @Test
    void contextLoads() {
        System.out.println(person);
    }

}</code></pre>
<h3 id="properties"><a href="#properties" class="headerlink" title="properties"></a>properties</h3><p>上述yaml配置等效：</p>
<pre><code class="properties">person.user-name=张三
person.age=18
person.birthday=2020/02/26
person.sex=true
person.map.k1=v1
person.map.k2=v2
person.list=item1,item2,item3
person.cat.name=猫咪
person.cat.age=2</code></pre>
<blockquote>
<p>当工程中同时有两种配置文件时，将以properties优先</p>
</blockquote>
<h4 id="中文乱码问题"><a href="#中文乱码问题" class="headerlink" title="中文乱码问题"></a>中文乱码问题</h4><p>如果使用properties配置时中文数据出现乱码，在设置中找到<code>File Encoding</code>，将配置文件字符设置为<code>UTF-8</code>，并且勾选 <code>Transparent native-to-ascii conversion</code></p>
<p><img src="http://image.cheerway.fun/blog/20200226/1R5zyYPhc0sj.png?imageslim" alt="mark"></p>
<h2 id="配置文件两种注入方式的对比"><a href="#配置文件两种注入方式的对比" class="headerlink" title="配置文件两种注入方式的对比"></a>配置文件两种注入方式的对比</h2><p>配置文件值注入有两种方式，一个是Spring Boot的<code>@ConfigurationProperties</code>注解，另一个是spring原先的<code>@value</code>注解</p>
<table>
<thead>
<tr>
<th align="center"></th>
<th align="center">@Value</th>
<th align="center">@ConfigurationProperties</th>
</tr>
</thead>
<tbody><tr>
<td align="center">功能</td>
<td align="center">一个个指定注入</td>
<td align="center">批量注入配置文件中的属性</td>
</tr>
<tr>
<td align="center">松散绑定</td>
<td align="center">不支持</td>
<td align="center">支持</td>
</tr>
<tr>
<td align="center">SpEL</td>
<td align="center">支持</td>
<td align="center">不支持</td>
</tr>
<tr>
<td align="center">JSR303数据校验</td>
<td align="center">不支持</td>
<td align="center">支持</td>
</tr>
<tr>
<td align="center">复杂类型封装</td>
<td align="center">不支持</td>
<td align="center">支持</td>
</tr>
</tbody></table>
<h3 id="松散绑定"><a href="#松散绑定" class="headerlink" title="松散绑定"></a>松散绑定</h3><blockquote>
<p>例如Person中有<code>lastName</code>属性，在配置文件中可以写成<code>lastName</code>或<code>lastname</code>或<code>last-name</code>或<code>last_name</code>等等</p>
</blockquote>
<h3 id="SpEL"><a href="#SpEL" class="headerlink" title="SpEL"></a>SpEL</h3><blockquote>
<p>举个栗子：</p>
<p><code>配置文件</code></p>
<pre><code class="properties">person.name=#{2019-1998+1}</code></pre>
<p><code>Person类</code></p>
<pre><code class="java">/**
*    使用@ConfigurationProperties注解，会抛出异常
*/
@Component
@ConfigurationProperties(prefix = &quot;person&quot;)
public class Person {
    private Integer age;

----------------------------------------------------------------------------
/**
*    使用@Value注解，正常注入
*/
    @Component
public class Person {
    @Value(&quot;${person.age}&quot;)
    private Integer age;</code></pre>
</blockquote>
<h3 id="JSR303数据校验"><a href="#JSR303数据校验" class="headerlink" title="JSR303数据校验"></a>JSR303数据校验</h3><p><code>@ConfigurationProperties</code>注解可以使用JSR303数据校验</p>
<p><img src="http://image.cheerway.fun/blog/20200226/pnGDtGwVtNhx.png?imageslim" alt="mark"></p>
<p><code>@Value</code>注解不支持数据校验</p>
<p><img src="http://image.cheerway.fun/blog/20200226/evrd48vQG7bk.png?imageslim" alt="mark"></p>
<h2 id="配置的其他知识"><a href="#配置的其他知识" class="headerlink" title="配置的其他知识"></a>配置的其他知识</h2><h3 id="PropertySource"><a href="#PropertySource" class="headerlink" title="@PropertySource"></a>@PropertySource</h3><p><code>@PropertySource</code>注解的作用是加载指定位置的配置文件，其指可以是数组，即可以加载多个配置文件</p>
<p>SpringBoot默认加载的配置文件名称是<code>application</code>，如果配置文件名不是这个是不会被容器加载的。</p>
<p><img src="http://image.cheerway.fun/blog/20200226/4MturEUqwdXc.png?imageslim" alt="mark"></p>
<blockquote>
<p>使用这个注解加载配置文件就需要配置类使用@component等注解而不是等待@EnableConfigurationProperties激活，而且不支持yaml，只能是properties</p>
</blockquote>
<h3 id="ImportResource"><a href="#ImportResource" class="headerlink" title="@ImportResource"></a>@ImportResource</h3><p><code>@ImportResource</code>注解用于导入Spring的配置文件，让配置文件里面的内容生效；(就是以前写的springmvc.xml、applicationContext.xml)</p>
<p>Spring Boot里面没有Spring的配置文件，我们自己编写的配置文件，也不能自动识别；</p>
<p><img src="http://image.cheerway.fun/blog/20200226/LiTBIgzvujgf.png?imageslim" alt="mark"></p>
<p>想让Spring的配置文件生效，加载进来；必须把@<strong>ImportResource</strong>标注在一个配置类上</p>
<blockquote>
<p>注意：此处应该把该注解加在入口类上而不是测试类</p>
</blockquote>
<p><img src="http://image.cheerway.fun/blog/20200226/j1iXGQCTSsRl.png?imageslim" alt="mark"></p>
<h3 id="Configuration"><a href="#Configuration" class="headerlink" title="@Configuration"></a>@Configuration</h3><p>这是SpringBoot推荐的添加注解的方式，并且推荐使用全注解的方式。</p>
<p>配置类加上<strong>@Configuration</strong>相当于 Spring配置文件。</p>
<h4 id="Bean"><a href="#Bean" class="headerlink" title="@Bean"></a>@Bean</h4><p>使用@Bean给容器中添加组件，其中组件名就是方法名。</p>
<p><img src="http://image.cheerway.fun/blog/20200226/s4PmyAVxd4Fh.png?imageslim" alt="mark"></p>
<h3 id="配置文件占位符"><a href="#配置文件占位符" class="headerlink" title="配置文件占位符"></a>配置文件占位符</h3><h4 id="随机值"><a href="#随机值" class="headerlink" title="随机值"></a>随机值</h4><pre><code class="java">${random.value}
${random.int}
${random.long}
${random.int(10)}
${random.int[1024,65536]}</code></pre>
<h4 id="引用配置好的其他属性"><a href="#引用配置好的其他属性" class="headerlink" title="引用配置好的其他属性"></a>引用配置好的其他属性</h4><p><img src="http://image.cheerway.fun/blog/20200226/UtEFBaPzUhtY.png?imageslim" alt="mark"></p>
<p>如果引用的属性没有值，可以使用<code>:</code>指定他的默认值</p>
<h3 id="Profile"><a href="#Profile" class="headerlink" title="Profile"></a>Profile</h3><p>Profile是Spring对不同环境提供不同配置功能的支持，可以通过激活、指定参数等方式快速切换环境</p>
<h4 id="多profile文件形式"><a href="#多profile文件形式" class="headerlink" title="多profile文件形式"></a>多profile文件形式</h4><p><img src="http://image.cheerway.fun/blog/20200226/dawLnttRyTks.png?imageslim" alt="mark"></p>
<p>此时运行程序，启动的是8080端口，但是我们可以自定义激活配置文件：</p>
<blockquote>
<p>举个栗子：如果在主配置文件中加入以下代码</p>
<pre><code class="properties">spring.profiles.active=dev</code></pre>
<p>再次运行程序，启动的则是我们配置的8081端口</p>
</blockquote>
<h4 id="yml支持多文档块"><a href="#yml支持多文档块" class="headerlink" title="yml支持多文档块"></a>yml支持多文档块</h4><p>我们可以不用那么多的配置文件，只需要将主配置文件分块即可：</p>
<pre><code class="yaml">server:
  port: 8080
spring:
  profiles:
    active: prod
---
server:
  port: 8081
spring:
  profiles: dev
---
server:
  port: 8082
spring:
  profiles: prod</code></pre>
<h4 id="激活指定profile总结"><a href="#激活指定profile总结" class="headerlink" title="激活指定profile总结"></a>激活指定profile总结</h4><ol>
<li><p>在配置文件中指定 spring.profiles.active=dev（如上）</p>
</li>
<li><p>项目打包后在命令行启动</p>
<pre><code class="shell">java -jar xxx.jar --spring.profiles.active=dev;</code></pre>
</li>
<li><p>虚拟机参数</p>
<p><img src="http://image.cheerway.fun/blog/20200226/fyV8zRiwJYjb.png?imageslim" alt="mark"></p>
</li>
</ol>
<h3 id="配置文件加载位置"><a href="#配置文件加载位置" class="headerlink" title="配置文件加载位置"></a>配置文件加载位置</h3><p>springboot 启动会扫描以下位置的application.properties或者application.yml文件作为Spring boot的默认配置文件</p>
<blockquote>
<pre><code>file: ./config/

file: ./

classpath: /config/

classpath: /</code></pre></blockquote>
<p>优先级由高到底，高优先级的配置会覆盖低优先级的配置（优先级低的先加载）；</p>
<p>SpringBoot会从这四个位置全部加载主配置文件；<strong>互补配置</strong>；</p>
<p><img src="http://image.cheerway.fun/blog/20200226/WHmqeGpljphn.png?imageslim" alt="mark"></p>
<blockquote>
<p>项目根目录下的配置文件maven在打包时不会编译进去，需要更改一下配置</p>
</blockquote>
<pre><code class="xml">&lt;resources&gt;
    &lt;resource&gt;
        &lt;directory&gt;.&lt;/directory&gt;
        &lt;filtering&gt;true&lt;/filtering&gt;
        &lt;includes&gt;
            &lt;include&gt;**/*.properties&lt;/include&gt;
            &lt;include&gt;**/*.yaml&lt;/include&gt;
        &lt;/includes&gt;
    &lt;/resource&gt;
&lt;/resources&gt;</code></pre>
<blockquote>
<p>还可以使用<code>spring.config.location</code>参数指定默认配置文件的位置</p>
<p><strong>项目打包好以后，我们可以使用命令行参数的形式，启动项目的时候来指定配置文件的新位置；指定配置文件和默认加载的这些配置文件共同起作用形成互补配置；</strong></p>
<pre><code class="shell">java -jar xxx.jar --spring.config.location=/home/cheerway/application.yaml</code></pre>
</blockquote>
<h3 id="外部配置加载顺序"><a href="#外部配置加载顺序" class="headerlink" title="外部配置加载顺序"></a>外部配置加载顺序</h3><p><strong>SpringBoot也可以从以下位置加载配置； 优先级从高到低；高优先级的配置覆盖低优先级的配置，所有的配置会形成互补配置</strong></p>
<ol>
<li><p><strong>命令行参数</strong> </p>
<p>所有的配置都可以在命令行上进行指定</p>
<pre><code>java -jar xxx.jar --server.port=8087  --server.context-path=/abcCopy to clipboardErrorCopied</code></pre><p>多个配置用空格分开； –配置项=值</p>
</li>
<li><p>来自java:comp/env的JNDI属性 ⤴️</p>
</li>
<li><p>Java系统属性（System.getProperties()） ⤴️</p>
</li>
<li><p>操作系统环境变量 ⤴️</p>
</li>
<li><p>RandomValuePropertySource配置的random.*属性值 ⤴️</p>
</li>
</ol>
<p><strong>由jar包外向jar包内进行寻找；</strong></p>
<p><strong>再来加载不带profile</strong></p>
<ol>
<li><strong>jar包外部的<code>application.properties</code>或<code>application.yml</code>(不带spring.profile)配置文件</strong> ⤴️</li>
<li>*<em>jar包内部的<code>application.properties</code>或<code>application.yml</code>(不带spring.profile)配置文件 *</em>⤴️</li>
</ol>
<p><strong>优先加载带profile</strong></p>
<ol>
<li><p><strong>jar包外部的<code>application-{profile}.properties</code>或<code>application.yml</code>(带spring.profile)配置文件</strong> ⤴️</p>
</li>
<li><p>*<em>jar包内部的<code>application-{profile}.properties</code>或<code>application.yml</code>(带spring.profile)配置文件 *</em>⤴️</p>
</li>
<li><p>@Configuration注解类上的@PropertySource ⤴️</p>
</li>
<li><p>通过SpringApplication.setDefaultProperties指定的默认属性 ⤴️</p>
</li>
</ol>
<p>所有支持的配置加载来源，请参考<a href="https://docs.spring.io/spring-boot/docs/2.2.1.RELEASE/reference/htmlsingle/#boot-features-external-config" target="_blank" rel="noopener">官方文档</a>。</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot-HelloWorld</title>
    <url>/2020/ck71sexg50000cstn3hy6dnen/</url>
    <content><![CDATA[<h1 id="SpringBoot-HelloWorld"><a href="#SpringBoot-HelloWorld" class="headerlink" title="SpringBoot-HelloWorld"></a>SpringBoot-HelloWorld</h1><h2 id="1-为什么要用SpringBoot"><a href="#1-为什么要用SpringBoot" class="headerlink" title="1. 为什么要用SpringBoot"></a>1. 为什么要用SpringBoot</h2><p>相信用过SSM框架的小伙伴都知道，在使用过程中我们必须配置大量的.xml文件来整合各个框架并对其参数进行配置，这项工作对于大型项目来说苦不堪言。</p>
<p>于是Pivotal 团队提供了全新的框架Spring Boot，其设计目的是用来简化新 Spring 应用的初始搭建以及开发过程。该框架采用“习惯优于配置”的方式开发，可以快速构建Spring应用。</p>
<p>SpringBoot 主要有以下优点：</p>
<blockquote>
<ul>
<li>快速创建独立运行的Spring项目以及与主流框架集成</li>
<li>使用嵌入式的Servlet容器，应用无需打成WAR包</li>
<li>starters自动依赖与版本控制</li>
<li>大量的自动配置，简化开发，也可修改默认值</li>
<li>无需配置XML，无代码生成，开箱即用</li>
<li>准生产环境的运行时应用监控</li>
<li>与云计算的天然集成</li>
</ul>
</blockquote>
<p><code>虽然Spring Boot采用“约定大于配置”的方式来简化开发，但是对于不了解默认约定的小白来说可能并不友好</code></p>
<h2 id="2-创建SpringBoot应用的方式"><a href="#2-创建SpringBoot应用的方式" class="headerlink" title="2. 创建SpringBoot应用的方式"></a>2. 创建SpringBoot应用的方式</h2><h3 id="1-使用官网的初始化入口"><a href="#1-使用官网的初始化入口" class="headerlink" title="1. 使用官网的初始化入口"></a>1. <a href="https://start.spring.io/" target="_blank" rel="noopener">使用官网的初始化入口</a></h3><p><strong>在如下界面中填写相关信息</strong></p>
<p><img src="http://image.cheerway.fun/blog/20200225/n3Mhr88xwrEQ.png?imageslim" alt="avatar"></p>
<p>然后点击Generate即可生成一个SpringBoot项目，之后用IDEA打开即可。</p>
<h3 id="２-使用IDEA提供的Spring-Initialize创建项目"><a href="#２-使用IDEA提供的Spring-Initialize创建项目" class="headerlink" title="２. 使用IDEA提供的Spring Initialize创建项目"></a>２. 使用IDEA提供的Spring Initialize创建项目</h3><blockquote>
<p>以下步骤需要联网</p>
</blockquote>
<p><strong>本项目使用的环境:</strong> </p>
<table>
<thead>
<tr>
<th align="center">工具</th>
<th align="center">版本</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Maven</td>
<td align="center">3.3+</td>
</tr>
<tr>
<td align="center">JDK</td>
<td align="center">1.8</td>
</tr>
</tbody></table>
<ol>
<li>创建项目时选择Spring Initializr</li>
</ol>
<p>   <img src="http://image.cheerway.fun/blog/20200225/1Ka1VrhRc8os.png?imageslim" alt=""></p>
<ol start="2">
<li>完善项目的相关信息</li>
</ol>
<p>   <img src="http://image.cheerway.fun/blog/20200225/T1oCIIm5uaql.png?imageslim" alt=""></p>
<ol start="3">
<li>选择需要加入的依赖，本项目中我们只需要添加web</li>
</ol>
<p>   <img src="http://image.cheerway.fun/blog/20200225/ICaHQrAJfK93.png?imageslim" alt=""></p>
<ol start="4">
<li><p>创建完成以后IDEA会联网帮我们创建Spring Boot项目，如下图</p>
<p><img src="http://image.cheerway.fun/blog/20200225/cM7LLvgDWitK.png?imageslim" alt=""></p>
</li>
</ol>
<p><strong>默认生成的Spring Boot项目</strong></p>
<ul>
<li><p>主程序已经生成好了，我们只需要完成我们自己的逻辑</p>
</li>
<li><p><code>resources</code>文件夹中目录结构</p>
<ul>
<li><code>static</code>：保存所有的静态资源； js、css、images；</li>
<li><code>templates</code>：保存所有的模板页面；（Spring Boot默认jar包使用嵌入式的Tomcat，<code>默认</code>不支持JSP页面）；可以使用模板引擎（freemarker、thymeleaf）；</li>
<li><code>application.properties</code>：Spring Boot应用的配置文件；可以修改一些默认设置；</li>
</ul>
</li>
</ul>
<h2 id="3-完善HelloWorld项目"><a href="#3-完善HelloWorld项目" class="headerlink" title="3. 完善HelloWorld项目"></a>3. 完善HelloWorld项目</h2><h3 id="1-编写Controller"><a href="#1-编写Controller" class="headerlink" title="1. 编写Controller"></a>1. 编写Controller</h3><p>在主包下新建controller包并且在该包中新建一个class文件, 文件名为: HelloController</p>
<pre><code class="Java">package com.cheerway.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

@Controller
public class HelloController {

    @ResponseBody
    @RequestMapping(&quot;/hello&quot;)
    public String hello(){
        return &quot;Hello&quot;;
    }
}</code></pre>
<h3 id="2-运行项目"><a href="#2-运行项目" class="headerlink" title="2. 运行项目"></a>2. 运行项目</h3><p>然后我们可以打开自动生成的启动类, 运行该类可以看到控制台输出的Log</p>
<p><img src="http://image.cheerway.fun/blog/20200225/YrqIsHs5BXJ3.png?imageslim" alt=""></p>
<p>最后用浏览器访问<code>localhost:8080/hello</code>, 就可以看到项目运行的结果</p>
<p><img src="http://image.cheerway.fun/blog/20200225/Pw0G9ER8eJmA.png?imageslim" alt=""></p>
<h3 id="3-简化部署"><a href="#3-简化部署" class="headerlink" title="3. 简化部署"></a>3. 简化部署</h3><ol>
<li><p>Maven插件</p>
<p>该插件可以帮我们把springboot项目打成jar包, 达到快速部署的目的</p>
<pre><code class="xml">&lt;build&gt;
        &lt;plugins&gt;
            &lt;plugin&gt;
                &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
                &lt;artifactId&gt;spring-boot-maven-plugin&lt;/artifactId&gt;
            &lt;/plugin&gt;
        &lt;/plugins&gt;
    &lt;/build&gt;</code></pre>
</li>
<li><p>使用mvn pakage命令进行打包</p>
</li>
<li><p>使用java -jar命令运行该jar包</p>
</li>
</ol>
<h2 id="4-HelloWorld入门程序探究"><a href="#4-HelloWorld入门程序探究" class="headerlink" title="4. HelloWorld入门程序探究"></a>4. HelloWorld入门程序探究</h2><p><strong>依赖</strong></p>
<pre><code class="xml">&lt;parent&gt;
        &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
        &lt;artifactId&gt;spring-boot-starter-parent&lt;/artifactId&gt;
        &lt;version&gt;2.2.4.RELEASE&lt;/version&gt;
        &lt;relativePath/&gt; &lt;!-- lookup parent from repository --&gt;
&lt;/parent&gt;
这是我们项目依赖的父项目,他的父项目是
&lt;parent&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-dependencies&lt;/artifactId&gt;
    &lt;version&gt;2.2.4.RELEASE&lt;/version&gt;
    &lt;relativePath&gt;../../spring-boot-dependencies&lt;/relativePath&gt;
  &lt;/parent&gt;
这个才是管理SpringBoot全部版本的项目</code></pre>
<p><strong>启动器</strong></p>
<pre><code class="xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
&lt;/dependency&gt;</code></pre>
<p><strong>spring-boot-starter</strong>: spring-boot场景启动器；帮我们导入了web模块正常运行所依赖的组件；</p>
<p>Spring Boot将所有的功能场景都抽取出来，做成一个个的starters（启动器），只需要在项目里面引入这些starter相关场景的所有依赖都会导入进来。要用什么功能就导入什么场景的启动器</p>
<p><strong>主程序</strong></p>
<pre><code class="java">package com.cheerway;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * @SpringBootApplication 说明这是一个SpringBoot应用的入口
 */
@SpringBootApplication
public class HelloWorldApplication {

    public static void main(String[] args) {
        SpringApplication.run(HelloWorldApplication.class, args);
    }

}</code></pre>
<ul>
<li><p><code>@SpringBootApplication</code>: Spring Boot应用标注在某个类上说明这个类是SpringBoot的主配置类，SpringBoot就应该运行这个类的main方法来启动SpringBoot应用；</p>
<p>该注解其实是一个组合注解, 查看源码: </p>
<pre><code class="java">@Target({ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Inherited
@SpringBootConfiguration
@EnableAutoConfiguration
@ComponentScan(
    excludeFilters = {@Filter(
    type = FilterType.CUSTOM,
    classes = {TypeExcludeFilter.class}
), @Filter(
    type = FilterType.CUSTOM,
    classes = {AutoConfigurationExcludeFilter.class}
)}
)</code></pre>
<ul>
<li><p><code>@SpringBootConfiguration</code>: Spring Boot的配置类; 标注在某个类上，表示这是一个Spring Boot的配置类；</p>
<p>再进入该注解的源码可以看到我们的”老朋友”;</p>
<pre><code class="java">@Target({ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Configuration</code></pre>
<ul>
<li><p><code>@Configuration</code> 这是Spring中配置类的注解</p>
<pre><code class="java">@Target({ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Component
public @interface Configuration </code></pre>
<p>配置类也是容器中的一个组件, 因此其源码又有@Component</p>
</li>
</ul>
</li>
</ul>
</li>
<li><p><code>@EnableAutoConfiguration</code>: 自动配置注解</p>
<p>以前我们需要配置的东西，Spring Boot帮我们自动配置；@<strong>EnableAutoConfiguration</strong>告诉SpringBoot开启自动配置功能；这样自动配置才能生效；</p>
<pre><code class="java">@Target({ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Inherited
@AutoConfigurationPackage
@Import({AutoConfigurationImportSelector.class})
public @interface EnableAutoConfiguration</code></pre>
<ul>
<li><p><code>@AutoConfigurationPackage</code>: 自动配置包</p>
<pre><code class="java">@Target({ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Inherited
@Import({Registrar.class})
public @interface AutoConfigurationPackage</code></pre>
<ul>
<li><p><code>@import</code>注解：Spring的底层注解@Import，给容器中导入一个组件</p>
<p>导入的组件由<code>org.springframework.boot.autoconfigure.AutoConfigurationPackages.Registrar</code>将主配置类（@SpringBootApplication标注的类）的所在包及下面所有子包里面的所有组件扫描到Spring容器；</p>
</li>
</ul>
</li>
<li><p><code>@Import({AutoConfigurationImportSelector.class})</code></p>
<p><code>AutoConfigurationImportSelector.class</code>将所有需要导入的组件以全类名的方式返回；这些组件就会被添加到容器中；会给容器中导入非常多的自动配置类（xxxAutoConfiguration）；就是给容器中导入这个场景需要的所有组件，并配置好这些组件；有了自动配置类，免去了我们手动编写配置注入功能组件等的工作;</p>
</li>
</ul>
<p>Spring Boot在启动的时候从类路径下的META-INF/spring.factories中获取EnableAutoConfiguration指定的值，将这些值作为自动配置类导入到容器中，自动配置类就生效，帮我们进行自动配置工作；以前我们需要自己配置的东西，自动配置类都帮我们完成了；</p>
</li>
</ul>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>大数据线性回归预测学生成绩</title>
    <url>/2019/lr-student/</url>
    <content><![CDATA[<p>通过训练集训练和测试集测试来生成多个线性模型，从而预测学生成绩，本文所有代码请点击<a href="https://github.com/wavecao/BigDataPredicteGrades" target="_blank" rel="noopener">Github</a></p>
<h2 id="1-实验数据背景"><a href="#1-实验数据背景" class="headerlink" title="1. 实验数据背景"></a>1. 实验数据背景</h2><h3 id="1-1-数据来源"><a href="#1-1-数据来源" class="headerlink" title="1.1  数据来源"></a>1.1  数据来源</h3><p>本项目的数据来源于kaggle.com，数据集的名称为Student Grade Prediction，Paulo Cortez，Minho大学，葡萄牙吉马良斯，<a href="">http：//www3.dsi.uminho.pt/pcortez</a></p>
<h3 id="1-2-数据简介"><a href="#1-2-数据简介" class="headerlink" title="1.2  数据简介"></a>1.2  数据简介</h3><p>该数据接近了两所葡萄牙学校的中学学生的学习成绩。数据属性包括学生成绩，人口统计学，社会和与学校相关的特征），并通过使用学校报告和调查表进行收集。提供了两个关于两个不同学科表现的数据集：数学（mat）和葡萄牙语（por）</p>
<p>该数据集共有396条，每列33个属性，属性简介如下：</p>
<a id="more"></a>

<p>1.学校-学生学校（二进制：“ GP”-加布里埃尔·佩雷拉（Gabriel Pereira）或“ MS”-Mousinho da Silveira）</p>
<p>2.性别-学生的性别（二进制：“ F”-女性或“ M”-男性）</p>
<p>3.年龄-学生的年龄（数字：15至22）</p>
<p>4.地址-学生的家庭住址类型（二进制：“ U”-城市或“ R”-农村）</p>
<p>5.famsize-家庭大小（二进制：“ LE3”-小于或等于3或“ GT3”-大于3）</p>
<p>6.Pstatus-父母的同居状态（二进制：“ T”-同居或“ A”-分开）</p>
<p>7.Medu-母亲的教育（数字：0-无，1-初等教育（四年级），2 – 5至9年级，3 –中等教育或4 –高等教育）</p>
<p>8.Fedu-父亲的教育（数字：0-无，1-初等教育（四年级），2 – 5至9年级，3 –中等教育或4 –高等教育）</p>
<p>9.Mjob-母亲的工作（名义：“教师”，“与健康”有关的，民事“服务”（例如行政或警察），“在家”或“其他”）</p>
<p>10.Fjob-父亲的工作（名义：“教师”，“与健康”相关的，民事“服务”（例如行政或警察），“在家”或“其他”）</p>
<p>11.理由-选择这所学校的理由（名义：接近“家”，学校“声誉”，“课程”偏好或“其他”）</p>
<p>12.监护人-学生的监护人（名词：“母亲”，“父亲”或“其他”）</p>
<p>13.traveltime-学校到学校的旅行时间（数字：1-&lt;15分钟，2-15至30分钟，3-30分钟至1小时或4-&gt; 1小时）</p>
<p>14.学习时间-每周学习时间（数字：1-&lt;2小时，2-2至5小时，3-5至10小时或4-&gt; 10小时）</p>
<p>15.失败-过去类失败的次数（数字：如果1 &lt;= n &lt;3，则为n，否则为4）</p>
<p>16.schoolup-额外的教育支持（二进制：是或否）</p>
<p>17.famsup-家庭教育支持（二进制：是或否）</p>
<p>18.付费-课程主题内的额外付费课程（数学或葡萄牙语）（二进制：是或否）</p>
<p>19.活动-课外活动（二进制：是或否）</p>
<p>20.托儿所-上托儿所（二进制：是或否）</p>
<p>21.更高-想要接受高等教育（二进制：是或否）</p>
<p>22.互联网-在家上网（二进制：是或否）</p>
<p>23.浪漫-具有浪漫关系（二进制：是或否）</p>
<p>24.家族-家庭关系的质量（数字：从1-非常差到5-极好）</p>
<p>25.空闲时间-放学后的空闲时间（数字：从1-非常低到5-非常高）</p>
<p>26.外出-与朋友外出（数字：从1-非常低到5-非常高）</p>
<p>27.Dalc-工作日酒精消耗（数字：从1-非常低到5-非常高）</p>
<p>28.Walc-周末酒精消耗（数字：从1-非常低至5-非常高）</p>
<p>29.健康-当前的健康状况（数字：从1-非常差到5-非常好）</p>
<p>30.缺勤-缺勤人数（数字：0到93）</p>
<p>这些成绩与课程主题（数学或葡萄牙语）相关：</p>
<p>31.G1-第一期成绩（数字：0至20）</p>
<p>32.G2-第二学期成绩（数字：0至20）</p>
<p>33.G3-最终成绩（数字：0到20，输出目标）</p>
<h2 id="2-研究思路"><a href="#2-研究思路" class="headerlink" title="2. 研究思路"></a>2. 研究思路</h2><p>数据集中存在不少非数值型数据，所以在进行建模之前要进行数据清洗，主要是抛弃掉一些非相关性数据以及缺失值处理，对数据进行分析的时候要注意其中是否有缺失值。</p>
<p>一些机器学习算法能够处理缺失值，比如神经网络，一些则不能。对于缺失值，一般有以下几种处理方法：</p>
<p>（1）如果数据集很多，但有很少的缺失值，可以删掉带缺失值的行；</p>
<p>（2）如果该属性相对学习来说不是很重要，可以对缺失值赋均值或者众数。</p>
<p>（3）对于标称属性，可以赋一个代表缺失的值，比如‘U0’。因为缺失本身也可能代表着一些隐含信息。</p>
<p>（4）使用回归 随机森林等模型来预测缺失属性的值。因为Age在该数据集里是一个相当重要的特征（先对Age进行分析即可得知），所以保证一定的缺失值填充准确率是非常重要的，对结果也会产生较大影响。一般情况下，会使用数据完整的条目作为模型的训练集，以此来预测缺失值。对于当前的这个数据，可以使用随机森林来预测也可以使用线性回归预测。这里使用随机森林预测模型，选取数据集中的数值属性作为特征（因为sklearn的模型只能处理数值属性，所以这里先仅选取数值特征，但在实际的应用中需要将非数值特征转换为数值特征）</p>
<p>数据规范化后方可进行数据分析，本项目将首先拿出一些特定属性，通过绘制图标的形式单独分析该属性反映出来的信息，试图分析该属性对结果的影响。</p>
<p>最后将对全部数据建立线性模型，并把数据集分为训练集和测试集，以测试模型的准确性。</p>
<h2 id="3-具体步骤"><a href="#3-具体步骤" class="headerlink" title="3. 具体步骤"></a>3. 具体步骤</h2><h3 id="3-1-数据总体分析"><a href="#3-1-数据总体分析" class="headerlink" title="3.1  数据总体分析"></a>3.1  数据总体分析</h3><p>利用padans库的read_csv函数，可以读取数据集：</p>
<pre><code class="python"># 初始化数据
plt.rcParams[&#39;font.sans-serif&#39;] = [&#39;SimHei&#39;]  # 中文字体设置-黑体
plt.rcParams[&#39;axes.unicode_minus&#39;] = False  # 解决保存图像是负号&#39;-&#39;显示为方块的问题
sns.set(font=&#39;SimHei&#39;)  # 解决Seaborn中文显示问题
student = pd.read_csv(&#39;student-mat.csv&#39;)</code></pre>
<p>格式化后展示如图3-1-1</p>
<p><img src="http://image.cheerway.fun/blog/20191221/ild5anDbNOI3.png?imageslim" alt=""></p>
<center>图3-1-1数据格式化</center>
读取数据后，可以利用describe方法对单个属性分析，分析结果包括平均值，最值，标准差等，以G3属性为例，结果如图3-1-2所示

<p><img src="http://image.cheerway.fun/blog/20191221/Rmp9PfDelzdP.png?imageslim" alt=""></p>
<center>图3-1-2 describe函数对单个属性分析</center>
### 3.2  利用图表分析属性

<h4 id="3-2-1各分数段学生计数"><a href="#3-2-1各分数段学生计数" class="headerlink" title="3.2.1各分数段学生计数"></a>3.2.1各分数段学生计数</h4><p>对学生期末成绩直接分析，可以得出成绩的大致分布，那么后文在分析其他因素对成绩的影响时也会更有针对性</p>
<pre><code class="python">  # 根据人数多少统计各分数段的学生人数 

 grade_counts = student[&#39;G3&#39;].value_counts().sort_values().plot.barh(width=.9,color=sns.color_palette(&#39;inferno&#39;,40)) 

 grade_counts.axes.set_title(&#39;各分数值的学生分布&#39;,fontsize=30) 

 grade_counts.set_xlabel(&#39;学生数量&#39;, fontsize=30) 

 grade_counts.set_ylabel(&#39;最终成绩&#39;, fontsize=30) 

 plt.show()</code></pre>
<p>绘制出的图像如图3-2-1所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/fmFn0DCWkNTo.png?imageslim" alt=""></p>
<center>图3-2-1 各分数段学生统计图</center>
#### 3.2.2 学生成绩分布直方图

<p>3.2.1节中的图像并没有告诉我们有价值的信息，也许我们真正需要的是成绩分布直方图，即把成绩作为一个坐标轴，代码如下：</p>
<pre><code class="python"># 从低到高展示成绩分布图 
grade_distribution = sns.countplot(student[&#39;G3&#39;]) 
grade_distribution.set_title(&#39;成绩分布图&#39;, fontsize=30) 
grade_distribution.set_xlabel(&#39;期末成绩&#39;, fontsize=20) 
grade_distribution.set_ylabel(&#39;人数统计&#39;, fontsize=20) 
plt.show() </code></pre>
<p>绘制出的图像如图3-2-2所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/JBzNlvOUolQM.png?imageslim" alt=""></p>
<center>图3-2-2 期末成绩分布图</center>
可以看出得10分和11分的学生数量很多，虽然这只是一个中等成绩，大部分学生的成绩分布在8-15分之间。更值得注意的是，居然有接近40个人得了0分，不由得让我怀疑0其实是null值，但是在检查之后发现，确实是0值，外国学生这么菜？

<h4 id="3-2-3-年龄因素分析"><a href="#3-2-3-年龄因素分析" class="headerlink" title="3.2.3 年龄因素分析"></a>3.2.3 年龄因素分析</h4><p>中学生正处在身体机能快速发育的时期，大脑亦是如此，不同智力水平的学生势必会得到不同的分数，因此有必要分析一下年龄对成绩的影响，并且考虑到性别因素。</p>
<p>以下代码可以得出性别的相关数据：</p>
<pre><code class="python"># 分析性别比例 

male_studs = len(student[student[&#39;sex&#39;] == &#39;M&#39;]) 

female_studs = len(student[student[&#39;sex&#39;] == &#39;F&#39;]) 

print(&#39;男同学数量:&#39;,male_studs) 

print(&#39;女同学数量:&#39;,female_studs)</code></pre>
<p>结果为：男同学数量: 187，女同学数量: 208。男女比例并不悬殊。</p>
<p>分析完性别因素之后，我们先来看看年龄比例的曲线图，代码如下：</p>
<pre><code class="python"># 分析年龄分布比例（曲线图） 

age_distribution = sns.kdeplot(student[&#39;age&#39;], shade=True) 

age_distribution.axes.set_title(&#39;学生年龄分布图&#39;, fontsize=30) 

age_distribution.set_xlabel(&#39;年龄&#39;, fontsize=20) 

age_distribution.set_ylabel(&#39;比例&#39;, fontsize=20) 

plt.show() </code></pre>
<p>生成的图像如图3-2-3所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/7F9uhyjs0CxO.png?imageslim" alt=""></p>
<center>图3-2-3 年龄占比分布图</center>
该图反映出一个重要的信息，那就是绝大部分学的年龄都在15-19岁之间，但是除此之外没有其他有价值的信息，甚至看不出各个年龄段的人数，最重要的是没有考虑年龄，因此我又绘制的分性别的年龄分布直方图，代码如下：

<pre><code class="python"># 分性别年龄分布图（柱状图） 

age_distribution_sex = sns.countplot(&#39;age&#39;, hue=&#39;sex&#39;, data=student) 

age_distribution_sex.axes.set_title(&#39;不同年龄段的学生人数&#39;, fontsize=30) 

age_distribution_sex.set_xlabel(&#39;年龄&#39;, fontsize=30) 

age_distribution_sex.set_ylabel(&#39;人数&#39;, fontsize=30) 

plt.show() </code></pre>
<p>绘制的图像如图3-2-4所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/af0Xk9BY2yE8.png?imageslim" alt=""></p>
<center>图3-2-4 分性别年龄分布图</center>
结果就是，其实大部分学生的年龄是在15-18岁之间，并且各个年龄段的男女比例基本正常。超过该年龄的学生不一定是离群值，而是有年级下降的学生。分析完年龄本身，接下就可以分析年龄对成绩的影响，首先作出各年龄段的成绩箱型图，代码如下：

<pre><code class="python"># 各年龄段的成绩箱型图 

age_grade_boxplot = sns.boxplot(x=&#39;age&#39;, y=&#39;G3&#39;, data=student) 

age_grade_boxplot.axes.set_title(&#39;年龄与分数&#39;, fontsize = 30) 

age_grade_boxplot.set_xlabel(&#39;年龄&#39;, fontsize = 20) 

age_grade_boxplot.set_ylabel(&#39;分数&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>得到的图像如图3-2-5所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/BDeffNFdGkli.png?imageslim" alt=""></p>
<center>图3-2-5 各年龄段的成绩箱型图</center>
绘制分布而非统计图将有助于我们更好地理解数据，代码如下：

<pre><code class="python"># 各年龄段的成绩分布图 

age_grade_swarmplot = sns.swarmplot(x=&#39;age&#39;, y=&#39;G3&#39;, data=student) 

age_grade_swarmplot.axes.set_title(&#39;年龄与分数&#39;, fontsize = 30) 

age_grade_swarmplot.set_xlabel(&#39;年龄&#39;, fontsize = 20) 

age_grade_swarmplot.set_ylabel(&#39;分数&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>得到的图像如图3-2-6所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/AY1BCX9ppuDq.png?imageslim" alt=""></p>
<center>图3-2-6 各年龄段的成绩分布图</center>
在15-18这个主要年龄段，学生成绩在各分数段的分布基本一致，我们可以得出学生年龄与成绩并没有直接的关系，或者说关系很弱。

<h4 id="3-2-4-学生城乡因素分析"><a href="#3-2-4-学生城乡因素分析" class="headerlink" title="3.2.4 学生城乡因素分析"></a>3.2.4 学生城乡因素分析</h4><p>既然年龄对成绩的影响不如预期那么明显，我们不妨再来分析一下学生地区因素对成绩的影响，在大部分地区，城乡教育资源都有不小的差距，教育质量也是如此，部分来自于乡下小学的孩子在步入城市中学后会不会落后于从小就在城市接受教育的孩子呢？</p>
<p>首先分析城乡学生的比例，代码如下：</p>
<pre><code class="python"># 城乡学生计数 

areas_countplot = sns.countplot(student[&#39;address&#39;]) 

areas_countplot.axes.set_title(&#39;城乡学生&#39;, fontsize = 30) 

areas_countplot.set_xlabel(&#39;家庭住址&#39;, fontsize = 20) 

areas_countplot.set_ylabel(&#39;计数&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>绘制的图像如图3-2-7所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/fTXqHT6GIlty.png?imageslim" alt=""></p>
<center>图3-2-7 城乡学生分布图</center>
​    大部分学生来自城市，并且城乡比例约为3 ：1。但是城市学生一定获得了更好的成绩吗？还需要进一步分析，代码如下：

<pre><code class="python">sns.kdeplot(student.loc[student[&#39;address&#39;] == &#39;U&#39;, &#39;G3&#39;], label=&#39;Urban&#39;, shade = True) 

sns.kdeplot(student.loc[student[&#39;address&#39;] == &#39;R&#39;, &#39;G3&#39;], label=&#39;Rural&#39;, shade = True) 

plt.title(&#39;城市学生获得了更好的成绩吗？&#39;, fontsize = 20) 

plt.xlabel(&#39;分数&#39;, fontsize = 20) 

plt.ylabel(&#39;占比&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>绘制出的图像如图3-2-8所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/J89RRLlMqbOW.png?imageslim" alt=""></p>
<center>图3-2-8 城乡学生成绩分布图</center>
城乡学生成绩分布曲线的差别微乎其微，可见家庭住址的不同没有对成绩造成太大的影响。

<h3 id="3-3-各属性与成绩的相关性分析"><a href="#3-3-各属性与成绩的相关性分析" class="headerlink" title="3.3  各属性与成绩的相关性分析"></a>3.3  各属性与成绩的相关性分析</h3><p>至此我们已经分析了年龄和地区这两个因素，然而并没有得到预期的结果——找出对成绩有影响的因素并分析原因，所以接下来我们综合分析各个因素与成绩之间的关系，希望会有好的结果。（注：仅限于数值属性）</p>
<h4 id="3-3-1相关性初步计算"><a href="#3-3-1相关性初步计算" class="headerlink" title="3.3.1相关性初步计算"></a>3.3.1相关性初步计算</h4><p>首先计算出各个属性与最终成绩G3的相关性，只需一行代码：</p>
<p>student.corr()[‘G3’].sort_values() </p>
<p>结果如下：</p>
<pre><code>\1.  failures   -0.360415 

\2.  age     -0.161579 

\3.  goout    -0.132791 

\4.  traveltime  -0.117142 

\5.  health    -0.061335 

\6.  Dalc     -0.054660 

\7.  Walc     -0.051939 

\8.  freetime   0.011307 

\9.  absences   0.034247 

\10. famrel    0.051363 

\11. studytime   0.097820 

\12. Fedu     0.152457 

\13. Medu     0.217147 

\14. G1      0.801468 

\15. G2      0.904868 

\16. G3      1.000000 

\17. Name: G3, dtype: float64</code></pre><p>绝大部分机器学习算法无法处理非数值数据，因此在进行机器学习之前，我们要对部分属性进行编码。</p>
<p>首先介绍一下编码方式，第一种是标签编码：</p>
<p>此方法会为每个类别分配一个标签（label）</p>
<table>
<thead>
<tr>
<th>Occupation</th>
<th>Label</th>
</tr>
</thead>
<tbody><tr>
<td>Programmer</td>
<td>0</td>
</tr>
<tr>
<td>Data  Scientist</td>
<td>1</td>
</tr>
<tr>
<td>Engineer</td>
<td>2</td>
</tr>
</tbody></table>
<p>​    标签编码的问题是整数的分配是随机的，并且每次运行函数时都会改变。 此外，模型可能会为更大的标签赋予更高的优先级。当我们只有2个唯一值时，可以使用标签编码。</p>
<p>第二种是独热编码：</p>
<p>它为每个类别创建一个新列，并且仅使用二进制值。独热编码的缺点是，如果分类变量具有许多类别，则特征数量可能会爆炸。为了解决这个问题，我们可以执行PCA（或其他降维方法），然后执行独热编码。</p>
<table>
<thead>
<tr>
<th>Occupation</th>
<th>Occupation_prog</th>
<th>Occupation_ds</th>
<th>Occupation_eng</th>
</tr>
</thead>
<tbody><tr>
<td>Programmer</td>
<td>1</td>
<td>0</td>
<td>0</td>
</tr>
<tr>
<td>Data  Scientist</td>
<td>0</td>
<td>1</td>
<td>0</td>
</tr>
<tr>
<td>Enginner</td>
<td>0</td>
<td>0</td>
<td>1</td>
</tr>
</tbody></table>
<h4 id="3-3-2-编码后的相关性计算"><a href="#3-3-2-编码后的相关性计算" class="headerlink" title="3.3.2 编码后的相关性计算"></a>3.3.2 编码后的相关性计算</h4><p>​    利用独热编码处理我们的数据集，然后再次寻找相关性，尽管G1和G2是学生的期末成绩，并且与期末成绩G3高度相关，但我们将其删除。没有G2和G1的情况下预测G3更加困难，但是这种预测更加有用，因为我们希望找到影响成绩的其他因素。代码如下：</p>
<pre><code class="python"># 选取G3属性值 

labels = student[&#39;G3&#39;] 

# 删除school，G1和G2属性 

student = student.drop([&#39;school&#39;, &#39;G1&#39;, &#39;G2&#39;], axis=&#39;columns&#39;) 

# 对离散变量进行独热编码 

student = pd.get_dummies(student) 

# 选取相关性最强的6个 

most_correlated = student.corr().abs()[&#39;G3&#39;].sort_values(ascending=False) 

most_correlated = most_correlated[:6] 

print(most_correlated) </code></pre>
<p>输出的结果如下：</p>
<pre><code>\1.  G3       1.000000 

\2.  failures    0.360415 

\3.  Medu      0.217147 

\4.  higher_yes   0.182465 

\5.  higher_no   0.182465 

\6.  age      0.161579 

\7.  Fedu      0.152457  

\8.  Name: G3, dtype: float64 </code></pre><h3 id="3-4-高相关度属性对成绩的影响"><a href="#3-4-高相关度属性对成绩的影响" class="headerlink" title="3.4  高相关度属性对成绩的影响"></a>3.4  高相关度属性对成绩的影响</h3><h4 id="3-4-1-失败次数对成绩的影响"><a href="#3-4-1-失败次数对成绩的影响" class="headerlink" title="3.4.1 失败次数对成绩的影响"></a>3.4.1 失败次数对成绩的影响</h4><p>​    代码如下：</p>
<pre><code class="python"># 失败次数成绩分布图 

failures_swarmplot = sns.swarmplot(x=student[&#39;failures&#39;],y=student[&#39;G3&#39;]) 

failures_swarmplot.axes.set_title(&#39;失败次数少的学生分数更高吗？&#39;, fontsize = 30) 

failures_swarmplot.set_xlabel(&#39;失败次数&#39;, fontsize = 20) 

failures_swarmplot.set_ylabel(&#39;最终成绩&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>生成的图像如图3-4-1所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/FPhpSE69gNUL.png?imageslim" alt=""></p>
<center>图3-4-1 失败次数对成绩的影响</center>
​    结果很明显，失败次数少的学生其成绩分布图更偏向于高分，可见失败次数少的学生更容易拿到高分，这或许是因为失败次数少的学生自信心更强。

<h4 id="3-4-2-双亲受教育水平对成绩的影响"><a href="#3-4-2-双亲受教育水平对成绩的影响" class="headerlink" title="3.4.2 双亲受教育水平对成绩的影响"></a>3.4.2 双亲受教育水平对成绩的影响</h4><p>​    代码如下：</p>
<pre><code class="python"># 双亲受教育水平的影响 

family_ed = student[&#39;Fedu&#39;] + student[&#39;Medu&#39;]  

family_ed_boxplot = sns.boxplot(x=family_ed,y=student[&#39;G3&#39;]) 

family_ed_boxplot.axes.set_title(&#39;双亲受教育水平的影响&#39;, fontsize = 30) 

family_ed_boxplot.set_xlabel(&#39;家庭教育水平(Mother + Father)&#39;, fontsize = 20) 

family_ed_boxplot.set_ylabel(&#39;最终成绩&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>生成的图像如图3-4-2所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/bL7WV6bLSlzd.png?imageslim" alt=""></p>
<center>图3-4-2 双亲受教育水平对成绩的影响</center>
​    可见在一定范围内，随着家庭教育水平的提升，学生的平均成绩也有所提升，但是在图表的两极却出现了规律的之外的东西，家庭受教育水平最低的学生，最终成绩高于不少其他水平的家庭，而家庭受教育水平最高的家庭，孩子的成绩却不是最高。原因暂时不明，且不排除数据错误的可能。

<h4 id="3-4-3-学生升学意愿对成绩的影响"><a href="#3-4-3-学生升学意愿对成绩的影响" class="headerlink" title="3.4.3 学生升学意愿对成绩的影响"></a>3.4.3 学生升学意愿对成绩的影响</h4><p>高等教育是绝对变量，其值为是和否。 由于我们使用了独热编码，因此已将其转换为2个变量。 因此，我们可以消除其中之一（因为这些值是互补的）。 我们消除Higher_no，因为Higher_yes更直观。</p>
<p>​    代码如下：</p>
<pre><code class="python"># 学生自己的升学意志对成绩的影响 

personal_wish = sns.boxplot(x = student[&#39;higher_yes&#39;], y=student[&#39;G3&#39;]) 

personal_wish.axes.set_title(&#39;学生升学意愿对成绩的影响&#39;, fontsize = 30) 

personal_wish.set_xlabel(&#39;更高级的教育 (1 = 是)&#39;, fontsize = 20) 

personal_wish.set_ylabel(&#39;最终成绩&#39;, fontsize = 20) 

plt.show() </code></pre>
<p>生成的图像如图3-4-3所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/H6V25vVk1T1V.png?imageslim" alt=""></p>
<p>图3-4-3 学生自己的升学意志对成绩的影响</p>
<p>显而易见，如果学生本人渴望受到更高的教育，其成绩也会更高一些，毕竟学习是自己的事情。</p>
<h3 id="3-5-大数据模型"><a href="#3-5-大数据模型" class="headerlink" title="3.5  大数据模型"></a>3.5  大数据模型</h3><h4 id="3-5-1-数据分割"><a href="#3-5-1-数据分割" class="headerlink" title="3.5.1   数据分割"></a>3.5.1   数据分割</h4><p>在大数据通过机器学习建立模型的过程中，不仅要使用大量的数据来训练模型，还需要一定数量的测试集验证模型的准确性，从而判断模型是否可用。因此，我们将数据集分割成训练集和测试集，比例分别为75%和25%。只需一行代码：</p>
<pre><code class="python">X_train, X_test, y_train, y_test = train_test_split(student, labels, test_size = 0.25, random_state=42) </code></pre>
<p>分割后我们便可以利用数据集进行线性回归模型的训练。</p>
<h4 id="3-5-2-训练之前的准备工作"><a href="#3-5-2-训练之前的准备工作" class="headerlink" title="3.5.2   训练之前的准备工作"></a>3.5.2   训练之前的准备工作</h4><p>代码中的注释已经足够详细，不做解释，直接贴代码：</p>
<pre><code class="python"># 计算平均绝对误差和均方根误差 

# MAE-平均绝对误差 

# RMSE-均方根误差 

def evaluate_predictions(predictions, true): 

    mae = np.mean(abs(predictions - true)) 

    rmse = np.sqrt(np.mean((predictions - true) ** 2)) 



    return mae, rmse 



 # 求中位数 

 median_pred = X_train[&#39;G3&#39;].median() 



 # 所有中位数的列表 

 median_preds = [median_pred **for** _ **in** range(len(X_test))] 



 # 存储真实的G3值以传递给函数 

 true = X_test[&#39;G3&#39;] 



# 展示基准的原始指标 

 mb_mae, mb_rmse = evaluate_predictions(median_preds, true) 

print(&#39;Median Baseline MAE: {:.4f}&#39;.format(mb_mae)) 

print(&#39;Median Baseline RMSE: {:.4f}&#39;.format(mb_rmse)) </code></pre>
<p>最后两句代码的运行结果为：</p>
<p>Median Baseline MAE: 3.7879</p>
<p>Median Baseline RMSE: 4.8252</p>
<h4 id="3-5-3-预测模型训练"><a href="#3-5-3-预测模型训练" class="headerlink" title="3.5.3   预测模型训练"></a>3.5.3   预测模型训练</h4><p>本项目将试图训练出六种模型，分别为：线性回归，ElasticNet回归，随机森林，极端随机数，支持向量机（SVM），梯度提升树。</p>
<p>代码如下：</p>
<pre><code class="python"># 通过训练集训练和测试集测试来生成多个线性模型
def evaluate(X_train, X_test, y_train, y_test):
    # 模型名称
    model_name_list = [&#39;Linear Regression&#39;, &#39;ElasticNet Regression&#39;,
                      &#39;Random Forest&#39;, &#39;Extra Trees&#39;, &#39;SVM&#39;,
                       &#39;Gradient Boosted&#39;, &#39;Baseline&#39;]
    X_train = X_train.drop(&#39;G3&#39;, axis=&#39;columns&#39;)
    X_test = X_test.drop(&#39;G3&#39;, axis=&#39;columns&#39;)

    # 实例化模型
    model1 = LinearRegression()
    model2 = ElasticNet(alpha=1.0, l1_ratio=0.5)
    model3 = RandomForestRegressor(n_estimators=100)
    model4 = ExtraTreesRegressor(n_estimators=100)
    model5 = SVR(kernel=&#39;rbf&#39;, degree=3, C=1.0, gamma=&#39;auto&#39;)
    model6 = GradientBoostingRegressor(n_estimators=50)

    # 结果数据框
    results = pd.DataFrame(columns=[&#39;mae&#39;, &#39;rmse&#39;], index = model_name_list)

    # 每种模型的训练和预测
    for i, model in enumerate([model1, model2, model3, model4, model5, model6]):
        model.fit(X_train, y_train)
        predictions = model.predict(X_test)

        # 误差标准
        mae = np.mean(abs(predictions - y_test))
        rmse = np.sqrt(np.mean((predictions - y_test) ** 2))

        # 将结果插入结果框
        model_name = model_name_list[i]
        results.loc[model_name, :] = [mae, rmse]

    # 中值基准度量
    baseline = np.median(y_train)
    baseline_mae = np.mean(abs(baseline - y_test))
    baseline_rmse = np.sqrt(np.mean((baseline - y_test) ** 2))

    results.loc[&#39;Baseline&#39;, :] = [baseline_mae, baseline_rmse]

    return results
results = evaluate(X_train, X_test, y_train, y_test)
print(results)</code></pre>
<p>结果如下表所示：</p>
<table>
<thead>
<tr>
<th>模型名称</th>
<th>MAE-平均绝对误差</th>
<th>RMSE-均方根误差</th>
</tr>
</thead>
<tbody><tr>
<td>Linear Regression</td>
<td>3.41954</td>
<td>4.27557</td>
</tr>
<tr>
<td>ElasticNet Regression</td>
<td>3.62976</td>
<td>4.58258</td>
</tr>
<tr>
<td>Random Forest</td>
<td>3.04111</td>
<td>3.77742</td>
</tr>
<tr>
<td>Extra Trees</td>
<td>3.14586</td>
<td>4.05109</td>
</tr>
<tr>
<td>SVM</td>
<td>3.50899</td>
<td>4.38375</td>
</tr>
<tr>
<td>Gradient Boosted</td>
<td>3.19128</td>
<td>4.00822</td>
</tr>
<tr>
<td>Baseline</td>
<td>3.78788</td>
<td>4.82523</td>
</tr>
</tbody></table>
<p>至此模型的训练已经完成，后续的分析将在第五节中呈现。</p>
<h2 id="4-分析与总结"><a href="#4-分析与总结" class="headerlink" title="4. 分析与总结"></a>4. 分析与总结</h2><p>模型训练完成之后，并非全部工作完成了，我们在第四节的最后训练出了多个模型，但是模型也有准确率之别，我们下一步就是要找出最合适的模型，代码如下:</p>
<pre><code class="python"># 找出最合适的模型
plt.figure(figsize=(12, 8))

# 平均绝对误差
ax =  plt.subplot(1, 2, 1)
results.sort_values(&#39;mae&#39;, ascending = True).plot.bar(y = &#39;mae&#39;, color = &#39;b&#39;, ax = ax, fontsize=20)
plt.title(&#39;平均绝对误差&#39;, fontsize=20) 
plt.ylabel(&#39;MAE&#39;, fontsize=20)

# 均方根误差
ax = plt.subplot(1, 2, 2)
results.sort_values(&#39;rmse&#39;, ascending = True).plot.bar(y = &#39;rmse&#39;, color = &#39;r&#39;, ax = ax, fontsize=20)
plt.title(&#39;均方根误差&#39;, fontsize=20) 
plt.ylabel(&#39;RMSE&#39;,fontsize=20)
plt.tight_layout()
plt.show()</code></pre>
<p>生成的图像如图4-1所示：</p>
<p><img src="http://image.cheerway.fun/blog/20191221/dIc1MXKhIIuA.png?imageslim" alt=""></p>
<center>图4-1 各模型误差柱状图</center>
显然，无论是平均绝对误差还是均方根误差，线性回归模型都是最小的。因此我们对它进行保存，以便于以后调用，代码很简单：

<p>*</p>
<pre><code class="python"># 保存线性回归模型*

model = LinearRegression()

model.fit(X_train, y_train)

filename = &#39;LR_Model&#39;

pickle.dump(model, open(filename, &#39;wb&#39;)) </code></pre>
<p>至此，建立大数据模型的工作已经全部完毕，并且保存了我们训练好的模型，以后如果想预测某个学生的成绩，只要将相关属性输入模型即可得出预测结果。</p>
<p>回过头来再想一下，大数据学习的初衷是数据分析，因此在建立模型之前，有必要对单个属性先进行一些分析，毕竟在某些数据集中可能会存在一些影响性大甚至是决定性的属性。但对属性分析不意味着随便拿属性分析，除了理性选择之外，我们最好提前把数据进行编码，然后分析各个属性的相关性，选取一些相关性较高的属性分析。比如本项目中，失败次数，学生个人升学意愿对学生的成绩影响非常明显。</p>
<p>需要强调的是，基于大数据的预测并不是绝对准确的，很多时候由于各种原因可能会造成离谱的误差，因此它只是我们预测结果的参考值。</p>
]]></content>
      <categories>
        <category>大数据</category>
        <category>算法</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>大数据</tag>
      </tags>
  </entry>
  <entry>
    <title>【算法】Python判断数字是否是回文数</title>
    <url>/2019/huiwenshu/</url>
    <content><![CDATA[<p><strong>回文数的定义：</strong>回文数是指正序（从左向右）和倒序（从右向左）读都是一样的整数。</p>
<a id="more"></a>

<p><strong>示例1：</strong></p>
<p>输入：121</p>
<p>输出：True</p>
<p><strong>示例2：</strong></p>
<p>输入：-121</p>
<p>输出：False</p>
<p>解释: 从左向右读, 为 -121 。 从右向左读, 为 121- 。因此它不是一个回文数。</p>
<p><strong>示例3：</strong></p>
<p>输入: 10</p>
<p>输出: false</p>
<p>解释: 从右向左读, 为 01 。因此它不是一个回文数。</p>
<p><strong>思路：</strong></p>
<p>映入脑海的第一个想法是将数字转换为字符串，并检查字符串是否为回文。但是，这需要额外的非常量空间来创建问题描述中所不允许的字符串。</p>
<p>第二个想法是将数字本身反转，但是这样做有一个风险，32位机器的int类型最大值为2147483647，反转后的数字可能超出范围造成溢出。</p>
<p>按照第二个想法，为了避免数字反转可能导致的溢出问题，为什么不考虑只反转 int\text{int}int 数字的一半？毕竟，如果该数字是回文，其后半部分反转后应该与原始数字的前半部分相同。</p>
<p>例如，输入 1221，我们可以将数字“1221”的后半部分从“21”反转为“12”，并将其与前半部分“12”进行比较，因为二者相同，我们得知数字 1221 是回文。</p>
<p><strong>算法：</strong></p>
<p>首先我们要处理一些特殊情况，负数不可能是回文数，首先要禁止负数进入。</p>
<p>然后再考虑如何反转数字，例如1221，首先1221%10就可以得到最后一位数字1，然后将1221/10得到122，再次122%10得到倒数第二位数字2，最后用1*10+2=12就得到了我们反转后的数字12，用12和原数字的前两位对比即可判断该数字是不是回文数。</p>
<p>还存在一个问题：我们怎么知道自己反转了一半呢？</p>
<p>我们将原始数字除以 10，然后给反转后的数字乘上 10，所以，当原始数字小于反转后的数字时，就意味着我们已经处理了一半位数的数字。</p>
<p><strong>代码实现：</strong></p>
<pre><code class="python">Python 

class Solution(object):    
    def isPalindrome(self, x):        
        &quot;&quot;&quot;        
        :type x: int        
        :rtype: bool        
        &quot;&quot;&quot;        
        if x&lt;0 or (x % 10 == 0 and x != 0):                        return False        
        y=0        
        while(x&gt;y):            
            y=x%10+y*10            
            x /= 10;        
        return x==y or x==y/10 </code></pre>
<pre><code class="java">Java 

class Solution {    
    public boolean isPalindrome(int x) {        
        if(x &lt; 0 || (x % 10 == 0 &amp;&amp; x != 0))                    return false;        
        int res = 0;        
        while(x &gt; res){            
            res = res * 10 + x % 10;            
            x /= 10;        
        }        
        return x == res || x == res / 10;    
    } 
} </code></pre>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>关于我</title>
    <url>/2019/about/</url>
    <content><![CDATA[<h3 id="我是菜鸟，欢迎赐教："><a href="#我是菜鸟，欢迎赐教：" class="headerlink" title="我是菜鸟，欢迎赐教："></a>我是菜鸟，欢迎赐教：</h3><p>Github: <a href="https://github.com/wavecao" target="_blank" rel="noopener">Wavecao</a></p>
<p>QQ: 2353769789</p>
<p>微信：C2353769789</p>
]]></content>
      <categories>
        <category>关于我</category>
      </categories>
      <tags>
        <tag>2019</tag>
      </tags>
  </entry>
</search>
